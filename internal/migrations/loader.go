//go:build !embed_migrations

package migrations

// LoadMigrations loads all migrations from the embedded filesystem
func (m *Migrator) LoadMigrations() ([]*Migration, error) {
	// For embedded migrations, use predefined list
	return allMigrations(), nil
}

// All returns the registered migrations, in registration order.
//
// It exists for tools/orgscope, which derives the tenant-scope census from
// the DDL rather than from a hand-maintained list. A hand-maintained list
// cannot see a table nobody remembered to add to it, and that blind spot is
// exactly how ispm_*, ai_agents and ai_recommendations were read and mutated
// across tenants until v138. The registry is the only place that knows every
// table this schema has, so it is what the lint reads.
func All() []*Migration { return allMigrations() }

// allMigrations returns the complete list of migrations
// This is generated from the migrations directory
func allMigrations() []*Migration {
	return []*Migration{
		{
			Version:     1,
			Name:        "initial_schema",
			Description: "Initial database schema with core tables",
			UpSQL:       initialSchemaUp,
			DownSQL:     initialSchemaDown,
		},
		{
			Version:     2,
			Name:        "oauth_oidc",
			Description: "OAuth 2.0 / OIDC tables",
			UpSQL:       oauthOIDCUp,
			DownSQL:     oauthOIDCDown,
		},
		{
			Version:     3,
			Name:        "scim",
			Description: "SCIM 2.0 provisioning tables",
			UpSQL:       scimUp,
			DownSQL:     scimDown,
		},
		{
			Version:     4,
			Name:        "governance",
			Description: "Governance tables for access reviews and policies",
			UpSQL:       governanceUp,
			DownSQL:     governanceDown,
		},
		{
			Version:     5,
			Name:        "mfa",
			Description: "Multi-factor authentication tables (TOTP, WebAuthn, Push)",
			UpSQL:       mfaUp,
			DownSQL:     mfaDown,
		},
		{
			Version:     6,
			Name:        "sessions",
			Description: "Session management tables",
			UpSQL:       sessionsUp,
			DownSQL:     sessionsDown,
		},
		{
			Version:     7,
			Name:        "applications",
			Description: "Application management tables",
			UpSQL:       applicationsUp,
			DownSQL:     applicationsDown,
		},
		{
			Version:     8,
			Name:        "audit_compliance",
			Description: "Audit and compliance tables",
			UpSQL:       auditComplianceUp,
			DownSQL:     auditComplianceDown,
		},
		{
			Version:     9,
			Name:        "indexes",
			Description: "Performance indexes",
			UpSQL:       indexesUp,
			DownSQL:     indexesDown,
		},
		{
			Version:     10,
			Name:        "seed_data",
			Description: "Initial seed data (admin user, roles, groups)",
			UpSQL:       seedDataUp,
			DownSQL:     seedDataDown,
		},
		{
			Version:     11,
			Name:        "identity_providers",
			Description: "External identity provider integration (OIDC/SAML)",
			UpSQL:       identityProvidersUp,
			DownSQL:     identityProvidersDown,
		},
		{
			Version:     12,
			Name:        "provisioning_rules",
			Description: "Provisioning rules and password reset tokens",
			UpSQL:       provisioningRulesUp,
			DownSQL:     provisioningRulesDown,
		},
		{
			Version:     13,
			Name:        "permissions",
			Description: "Permissions and role permissions tables",
			UpSQL:       permissionsUp,
			DownSQL:     permissionsDown,
		},
		{
			Version:     14,
			Name:        "system_settings",
			Description: "System settings and configuration",
			UpSQL:       systemSettingsUp,
			DownSQL:     systemSettingsDown,
		},
		{
			Version:     15,
			Name:        "directory_integrations",
			Description: "Directory integration tables",
			UpSQL:       directoryIntegrationsUp,
			DownSQL:     directoryIntegrationsDown,
		},
		{
			Version:     16,
			Name:        "proxy_routes",
			Description: "Zero Trust Access Proxy routes and sessions",
			UpSQL:       proxyRoutesUp,
			DownSQL:     proxyRoutesDown,
		},
		{
			Version:     17,
			Name:        "openziti",
			Description: "OpenZiti integration tables",
			UpSQL:       openzitiUp,
			DownSQL:     openzitiDown,
		},
		{
			Version:     18,
			Name:        "directory_sync",
			Description: "Directory sync state and logs",
			UpSQL:       directorySyncUp,
			DownSQL:     directorySyncDown,
		},
		{
			Version:     19,
			Name:        "conditional_access",
			Description: "Conditional access and risk engine tables",
			UpSQL:       conditionalAccessUp,
			DownSQL:     conditionalAccessDown,
		},
		{
			Version:     20,
			Name:        "api_keys_webhooks",
			Description: "API keys, webhooks, email verification, invitations",
			UpSQL:       apiKeysWebhooksUp,
			DownSQL:     apiKeysWebhooksDown,
		},
		{
			Version:     21,
			Name:        "access_requests",
			Description: "Access request and approval workflow tables",
			UpSQL:       accessRequestsUp,
			DownSQL:     accessRequestsDown,
		},
		{
			Version:     22,
			Name:        "security_alerts",
			Description: "Security alerts and anomaly detection",
			UpSQL:       securityAlertsUp,
			DownSQL:     securityAlertsDown,
		},
		{
			Version:     23,
			Name:        "password_management",
			Description: "Password history and credential rotation",
			UpSQL:       passwordManagementUp,
			DownSQL:     passwordManagementDown,
		},
		{
			Version:     24,
			Name:        "session_enhancements",
			Description: "Session management enhancements",
			UpSQL:       sessionEnhancementsUp,
			DownSQL:     sessionEnhancementsDown,
		},
		{
			Version:     25,
			Name:        "multitenancy",
			Description: "Multi-tenancy support with organizations",
			UpSQL:       multitenancyUp,
			DownSQL:     multitenancyDown,
		},
		{
			Version:     26,
			Name:        "reporting",
			Description: "Advanced reporting tables",
			UpSQL:       reportingUp,
			DownSQL:     reportingDown,
		},
		{
			Version:     27,
			Name:        "self_service",
			Description: "Self-service portal tables",
			UpSQL:       selfServiceUp,
			DownSQL:     selfServiceDown,
		},
		{
			Version:     28,
			Name:        "notifications",
			Description: "Notification system tables",
			UpSQL:       notificationsUp,
			DownSQL:     notificationsDown,
		},
		{
			Version:     29,
			Name:        "ziti_enhanced",
			Description: "OpenZiti enhanced features (posture checks, policy sync)",
			UpSQL:       zitiEnhancedUp,
			DownSQL:     zitiEnhancedDown,
		},
		{
			Version:     30,
			Name:        "user_roles_expires_at",
			Description: "Time-bound role assignments: adds the expires_at column already referenced by GenerateJWT and the role-expiry cleaner",
			UpSQL:       userRolesExpiresAtUp,
			DownSQL:     userRolesExpiresAtDown,
		},
		{
			Version:     31,
			Name:        "oauth_refresh_tokens_session_id",
			Description: "Adds the session_id column oauth_refresh_tokens has been silently failing INSERTs on since session-bound rotation landed; fixes the refresh-token grant",
			UpSQL:       oauthRefreshSessionIDUp,
			DownSQL:     oauthRefreshSessionIDDown,
		},
		{
			Version:     32,
			Name:        "privacy_tables",
			Description: "Brings privacy/GDPR tables (user_consents, data_subject_requests, privacy_retention_policies, privacy_assessments) under the migration runner — previously only created by deployments/docker/init-db.sql, so Helm/Terraform installs were missing them",
			UpSQL:       privacyTablesUp,
			DownSQL:     privacyTablesDown,
		},
		{
			Version:     33,
			Name:        "qr_login_sessions",
			Description: "Adds the qr_login_sessions table that internal/identity/passwordless.go has been INSERTing into since the QR-login feature shipped, but which no migration ever created — /oauth/qr-login/create was 500ing for every install on the first INSERT. Surfaced by the PR #126 integration tests.",
			UpSQL:       qrLoginSessionsUp,
			DownSQL:     qrLoginSessionsDown,
		},
		{
			Version:     34,
			Name:        "org_id_columns",
			Description: "Adds NULL-able org_id UUID columns + idx_<table>_org_id indexes to ~55 tables the v2.0 multi-tenancy design says must be org-scoped but that migration v025 didn't reach. Idempotent (IF NOT EXISTS). No backfill, no NOT NULL, no FK — those land in v035 and v036. Tables that are genuinely install-wide (organizations, permissions, system_settings, ip_threat_list, posture_check_types, policy_sync_state) are deliberately not scoped.",
			UpSQL:       orgIDColumnsUp,
			DownSQL:     orgIDColumnsDown,
		},
		{
			Version:     35,
			Name:        "org_id_backfill",
			Description: "Backfills the default organization UUID (00000000-0000-0000-0000-000000000010, created by v025) into every NULL org_id row across the ~55 tables v34 scoped. Idempotent (WHERE org_id IS NULL guards each UPDATE). Defensive INSERT at the top ensures the default org row exists even on installs that somehow skipped v25. No schema change, no constraint change, no behavioral change. Sets up v036 to add NOT NULL + FK.",
			UpSQL:       orgIDBackfillUp,
			DownSQL:     orgIDBackfillDown,
		},
		{
			Version:     36,
			Name:        "org_id_constraints",
			Description: "Final v1.6.0 foundation migration. For each of the 68 scoped tables: SET DEFAULT to default org UUID (preserves v1.6.0 ship gate — INSERTs that omit org_id silently land in default org), SET NOT NULL (schema correctness; v35 backfilled every row so this never rewrites data), ADD FK to organizations(id) ON DELETE RESTRICT (referential integrity), CREATE permissive RLS policy (placeholder — v1.8.0 ALTER POLICY tightens to a real org_id filter and ALTER TABLE ENABLE ROW LEVEL SECURITY activates enforcement). RLS itself is NOT enabled here; v1.8.0 owns activation.",
			UpSQL:       orgIDConstraintsUp,
			DownSQL:     orgIDConstraintsDown,
		},
		{
			Version:     37,
			Name:        "rls_activate",
			Description: "v1.8.0 RLS belt. Rewrites the 68 dormant pol_<t>_org_scope policies from USING(true) to a real tenant predicate — app.bypass_rls='on' OR org_id = NULLIF(current_setting('app.org_id', true), '')::uuid (fail-closed when the GUC is unset) — then ENABLE + FORCE ROW LEVEL SECURITY per table (FORCE because the app connects as the table owner). The per-request GUC is set at pool checkout from orgctx (internal/common/database); background/cross-org jobs opt into app.bypass_rls via orgctx.WithBypassRLS. Down reverts to USING(true) + RLS off.",
			UpSQL:       rlsActivateUp,
			DownSQL:     rlsActivateDown,
		},
		{
			Version:     38,
			Name:        "tenant_tables",
			Description: "Create tenant_branding, tenant_domains and tenant_settings in the versioned migration set. These back the multi-tenant login (per-org branding read by the public branding endpoint + OAuth login page) and domain-based tenant resolution. They previously lived only in deployments/docker/init-db.sql (docker-compose only), so managed-RDS/Helm deploys never had them. DDL is idempotent (IF NOT EXISTS) — a no-op on init-db.sql-bootstrapped clusters. Not placed under the RLS belt: both are read before any org context exists.",
			UpSQL:       tenantTablesUp,
			DownSQL:     tenantTablesDown,
		},
		{
			Version:     39,
			Name:        "device_trust_risk_tables",
			Description: "Create device_trust_requests, device_trust_settings, trusted_browsers and risk_policies in the versioned migration set. Their internal/identity handlers returned 500 on managed-RDS/Helm/migrate deploys because the tables existed only in deployments/docker/init-db.sql (same gap class as v38's tenant_* tables). DDL is idempotent. A wider init-db.sql↔migrations gap (~70 more tables) is tracked as separate follow-up.",
			UpSQL:       deviceTrustRiskUp,
			DownSQL:     deviceTrustRiskDown,
		},
		{
			Version:     40,
			Name:        "access_schema_reconcile",
			Description: "Fix the access-service App Publish feature on managed-RDS/Helm/migrate deploys: create published_apps, discovered_paths, service_features (init-db.sql-only tables, so /api/v1/access/apps 500'd) and add the ~12 proxy_routes columns the access code SELECTs (idp_id, route_type, remote_host/port, posture/risk/guacamole/browzer, …) that the migrations had drifted behind init-db.sql (so /api/v1/access/routes 500'd with 'column idp_id does not exist'). Idempotent.",
			UpSQL:       accessSchemaUp,
			DownSQL:     accessSchemaDown,
		},
		{
			Version:     41,
			Name:        "published_apps_public_host",
			Description: "Adds public_host + landing_path to published_apps for the one-click 'open internal app' publish flow (POST /api/v1/access/apps/:id/publish-app): public_host records the external host assigned to an app (UI display + idempotent re-publish); landing_path is where the launcher tile points for apps whose UI is not at the site root (default '/'). Idempotent (ADD COLUMN IF NOT EXISTS).",
			UpSQL:       oneClickAppsUp,
			DownSQL:     oneClickAppsDown,
		},
		{
			Version:     42,
			Name:        "reconcile_table_gap",
			Description: "Create init-db.sql-only / loose-SQL tables missing on managed-RDS/Helm/migrate installs (same class as v38–v40): abac_policies, certification_campaigns, ai_agents (init-db.sql), saml_sessions (010_add_saml_idp_tables.sql), remote_support_sessions (202605150004_remote_support.up.sql) — fixes 500s on the ABAC Policies / AI Agents / Certification Campaigns / Remote Support pages and the saml_sessions session-worker errors. Also adds certification_campaigns started_at/completed_at/total_items that the run code writes but no source created. Idempotent. (agents, recommendations, notification_digest_settings have no schema anywhere — genuinely unimplemented, tracked separately.)",
			UpSQL:       tableGapV42Up,
			DownSQL:     tableGapV42Down,
		},
		{
			Version:     43,
			Name:        "reconcile_agent_recommendation_digest_tables",
			Description: "Create the Agent Fleet / AI Recommendations / Notification Digest tables missing on managed-RDS/Helm/migrate installs (same init-db↔migrations gap as v38–v42): enrolled_agents (+ agent_posture_results, agent_enrollment_tokens) from the loose 2025*_enrolled_agents files; ai_recommendations + recommendation_history and the ai_agents children ai_agent_credentials/permissions/activity (v42 created ai_agents but not its children) from init-db.sql; notification_digests from init-db.sql. Fixes 500s on the Agent Fleet, AI Recommendations and Notification Center digest pages. Idempotent; verbatim DDL; not org-scoped (matches source + handlers).",
			UpSQL:       agentRecoDigestUp,
			DownSQL:     agentRecoDigestDown,
		},
		{
			Version:     44,
			Name:        "reconcile_kiosk_policy_tables",
			Description: "Final init-db↔migrations reconcile pass (after v38–v43): create kiosk_policies + kiosk_policy_assignments (loose file 202605150003) — fixes 500 on GET /api/v1/access/kiosk/policies — and policy_recommendations + compliance_gaps (loose file 017_add_genai_and_advanced_auth_tables) for the AI policy-suggestion / compliance-gap endpoints. Idempotent; verbatim DDL; not org-scoped (matches source + handlers).",
			UpSQL:       kioskPolicyV44Up,
			DownSQL:     kioskPolicyV44Down,
		},
		{
			Version:     45,
			Name:        "ziti_browzer_config",
			Description: "Create ziti_browzer_config (init-db.sql-only) so BrowZer bootstrap can persist its external-JWT-signer / auth-policy / dial-policy IDs + OIDC settings and GET /ziti/browzer/status reports configured state. Same init-db↔migrations gap as v42–v44. Idempotent.",
			UpSQL:       zitiBrowzerConfigUp,
			DownSQL:     zitiBrowzerConfigDown,
		},
		{
			Version:     46,
			Name:        "ziti_identities_group_attrs_synced_at",
			Description: "Add ziti_identities.group_attrs_synced_at (init-db.sql-only column) so the Ziti user-sync INSERT/resync works on migrated installs; its absence blocked BrowZer identity provisioning (externalId + auth-policy wiring). Same init-db↔migrations gap as v42–v45. Idempotent.",
			UpSQL:       zitiIdentitiesGroupSyncUp,
			DownSQL:     zitiIdentitiesGroupSyncDown,
		},
		{
			Version:     47,
			Name:        "proxy_routes_hosting_mode",
			Description: "Add proxy_routes.hosting_mode (identity|direct) for the Ziti reconciler; backfill browzer_enabled routes to 'direct', else 'identity'. Idempotent.",
			UpSQL:       hostingModeUp,
			DownSQL:     hostingModeDown,
		},
		{
			Version:     48,
			Name:        "proxy_routes_landing_path",
			Description: "Add proxy_routes.landing_path (default '/') — where a route's bare host should land; the BrowZer router emits a 302 to it when not '/'. Idempotent.",
			UpSQL:       landingPathUp,
			DownSQL:     landingPathDown,
		},
		{
			Version:     49,
			Name:        "applications_referential_integrity",
			Description: "Add applications.route_id (→proxy_routes) and applications.oauth_client_id (→oauth_clients), both ON DELETE CASCADE; backfill from the client_id string conventions so tiles/apps can't orphan. Idempotent, non-destructive.",
			UpSQL:       appReferentialIntegrityUp,
			DownSQL:     appReferentialIntegrityDown,
		},
		{
			Version:     50,
			Name:        "device_posture_results_upsert_key",
			Description: "Add UNIQUE INDEX device_posture_results(identity_id, check_id) so the agent→device posture bridge upserts one latest row per check. Idempotent.",
			UpSQL:       devicePostureUpsertKeyUp,
			DownSQL:     devicePostureUpsertKeyDown,
		},
		{
			Version:     51,
			Name:        "drop_zt_policies",
			Description: "Drop the dead ZTPolicy tables (zt_policies, zt_policy_versions). Never wired into any service; absent on real installs. Belt cleanup, idempotent (DROP ... IF EXISTS).",
			UpSQL:       ztPolicyDropUp,
			DownSQL:     ztPolicyDropDown,
		},
		{
			Version:     52,
			Name:        "reconcile_continuous_verify_columns",
			Description: "Add the init-db-only continuous-verify columns (proxy_sessions.last_verified_at/verification_failures/geo_country/geo_city/idp_id/device_trusted; user_sessions.device_trusted + index) so the continuous session verifier's query works on migrate-based installs. Idempotent; Down is a no-op.",
			UpSQL:       continuousVerifyColumnsUp,
			DownSQL:     continuousVerifyColumnsDown,
		},
		{
			Version:     53,
			Name:        "provision_openidx_app_role",
			Description: "Provision the openidx_app NOSUPERUSER NOBYPASSRLS runtime role with blanket DML grants + default privileges, so the v37 FORCE'd RLS policies enforce once the app cuts its DATABASE_URL over to it. Passwordless (set at deploy). Migrations/DDL stay on the owner. Idempotent.",
			UpSQL:       rlsAppRoleUp,
			DownSQL:     rlsAppRoleDown,
		},
		{
			Version:     54,
			Name:        "reconcile_initdb_table_gap",
			Description: "Create the 58 tables that existed only in deployments/docker/init-db.sql so managed-Postgres/RDS/Helm/migrate installs stop 500ing across MFA variants, SAML/social/federation, lifecycle, ISPM, audit archival, bulk-ops, biometric, passwordless, guacamole and ziti-fabric features (the broad init-db<->migrations reconcile after v38-v45's ~15). Verbatim DDL, idempotent (IF NOT EXISTS) so it is a no-op on docker-compose installs. Not under the v37 RLS belt (non-FORCE tables; openidx_app unaffected). TestInitDBParity now keeps this gap from recurring.",
			UpSQL:       reconcileTableGapUp,
			DownSQL:     reconcileTableGapDown,
		},
		{
			Version:     55,
			Name:        "lifecycle_policy_executions",
			Description: "Split the deprovisioning policy-run execution log out of the workflow lifecycle_executions table (a name collision: the workflow schema won, so deprovisioning's policy_id queries failed on every install). Creates lifecycle_policy_executions; init-db.sql's dead second block is renamed in lockstep and internal/admin/deprovisioning.go repointed. Idempotent; not under the v37 RLS belt.",
			UpSQL:       lifecyclePolicyExecUp,
			DownSQL:     lifecyclePolicyExecDown,
		},
		{
			Version:     56,
			Name:        "vault_store",
			Description: "PAM credential vault store (M1): vault_secrets, vault_secret_versions, vault_access_grants, vault_checkouts — org-scoped under the v37 FORCE-RLS belt. Envelope ciphertext lives only in vault_secret_versions. Rotation (credential_rotation_policies) is deferred to M1b. Idempotent; mirrored into init-db.sql so TestInitDBParity stays green.",
			UpSQL:       vaultStoreUp,
			DownSQL:     vaultStoreDown,
		},
		{
			Version:     57,
			Name:        "credential_rotation",
			Description: "PAM rotation engine (M1b): adds credential_rotation_policies (one-per-secret connector + schedule) and reconciles the dead credential_rotations table into a run ledger (org_id, policy_id, secret_id, version_from/to, connector_type, trigger, error_message, started_at, completed_at; service_account_id NOT NULL dropped). Both tables placed under the v37 FORCE-RLS belt. Idempotent; mirrored into init-db.sql so TestInitDBParity stays green.",
			UpSQL:       credentialRotationUp,
			DownSQL:     credentialRotationDown,
		},
		{
			Version:     58,
			Name:        "jit_approval_drift_fix",
			Description: "Create jit_grants and request_approval_chains — referenced by internal/governance (jit.go, jit_expiry.go, request.go) but never created by any migration or init-db.sql, so POST /api/v1/governance/requests and the JIT/escalation background workers 500 today. Columns match the code's exact SQL usage. Not under the v37 RLS belt (jit_grants isn't org-scoped in code; request_approval_chains is a child of access_requests reached via the RLS-scoped parent) — matches the v42-v55 reconcile precedent. Idempotent; mirrored into init-db.sql so TestInitDBParity stays green.",
			UpSQL:       jitDriftFixUp,
			DownSQL:     jitDriftFixDown,
		},
		{
			Version:     59,
			Name:        "guacamole_session_injection",
			Description: "PAM M3 session injection: adds vault_secret_id, inject_username, require_approval, record_session columns to guacamole_connections; creates guacamole_session_requests (pre-session approval lifecycle) and guacamole_sessions (active/ended session tracking + recording ledger), both under the v37 FORCE-RLS belt. guacamole_connections is not belted (only ALTERed). Idempotent; mirrored into init-db.sql so TestInitDBParity stays green.",
			UpSQL:       guacSessionsUp,
			DownSQL:     guacSessionsDown,
		},
		{
			Version:     60,
			Name:        "guacamole_transcripts",
			Description: "PAM M4 session assurance: adds transcript_path (TEXT) and transcript_generated_at (TIMESTAMPTZ) to guacamole_sessions so the session-assurance sweeper can track guacd-native transcript files independently of the recording blob. Idempotent (ADD COLUMN IF NOT EXISTS); mirrored into init-db.sql so TestInitDBParity stays green.",
			UpSQL:       guacTranscriptUp,
			DownSQL:     guacTranscriptDown,
		},
		{
			Version:     61,
			Name:        "attestation_org_isolation",
			Description: "Attestation tenant isolation: adds org_id (backfilled, NOT NULL, FK) to attestation_campaigns and attestation_items — created by v54 without org_id and queried without an org filter, a cross-org read/write exposure — and places both under the v37 FORCE-RLS belt (USING + WITH CHECK). Idempotent; mirrored into init-db.sql so TestInitDBParity stays green.",
			UpSQL:       attestationOrgIsolationUp,
			DownSQL:     attestationOrgIsolationDown,
		},
		{
			Version:     62,
			Name:        "reconcile_missing_tables",
			Description: "Reconcile referenced-but-uncreated tables (drift class of v58): creates admin_console_settings, auth_contexts, breach_incidents, breach_alerts — referenced by internal/admin (settings.go, continuous_auth.go, ibdr.go) but created by neither a migration nor init-db.sql, so their endpoints 500. Columns match the code's exact SQL usage. Not RLS-belted (code doesn't org-scope them; org_id/RLS is a separate follow-up). Idempotent; mirrored into init-db.sql so TestInitDBParity stays green.",
			UpSQL:       reconcileMissingTablesUp,
			DownSQL:     reconcileMissingTablesDown,
		},
		{
			Version:     63,
			Name:        "column_drift_reconcile",
			Description: "Reconcile init-db<->migrations COLUMN drift (paired with the new column-aware TestInitDBColumnParity). Fixes ziti_certificates (swaps the stale migration schema {cert_data NOT NULL, private_key_encrypted, ca_chain, expires_at, identity_id} for the code/init-db schema {cert_type, subject, issuer, serial_number, fingerprint, not_before, not_after, auto_renew, renewal_threshold_days, pem_data, status, associated_identity_id} + indexes) and adds columns init-db.sql adds via ALTER but no migration did, across application_sso_settings, directory_sync_state, ip_threat_list, oauth_clients, user_roles, user_sessions, users, ziti_service_policies. All idempotent; init-db.sql already defines these so it is unchanged.",
			UpSQL:       columnDriftReconcileUp,
			DownSQL:     columnDriftReconcileDown,
		},
		{
			Version:     64,
			Name:        "jit_approval_org_rls",
			Description: "Defense-in-depth: adds org_id (backfilled from the user/parent-request, NOT NULL, FK) to jit_grants and request_approval_chains and places both under the v37 FORCE-RLS belt (USING + WITH CHECK). v58 created them belt-less (org-scoped only via FKs/parent); the cross-org background sweeps already run under WithBypassRLS so the belt doesn't break them. Idempotent; mirrored into init-db.sql so TestInitDBParity stays green.",
			UpSQL:       jitApprovalOrgRLSUp,
			DownSQL:     jitApprovalOrgRLSDown,
		},
		{
			Version:     65,
			Name:        "webhook_secret_widen",
			Description: "Widen webhook_subscriptions.secret to TEXT so the HMAC signing secret can be stored AES-256-GCM-encrypted at rest (secretcrypt, tag encv1:) instead of plaintext — base64 ciphertext exceeds VARCHAR(255). Encryption is applied by the webhooks service (encrypt-on-write, prefix-aware decrypt-on-read); legacy plaintext rows read through unchanged and sign correctly, and are migrated by a one-off backfill tool (UpdateSubscription doesn't rewrite the secret). Idempotent; init-db.sql defines the column as TEXT directly.",
			UpSQL:       webhookSecretWidenUp,
			DownSQL:     webhookSecretWidenDown,
		},
		{
			Version:     66,
			Name:        "idp_client_secret_widen",
			Description: "Widen identity_providers.client_secret to TEXT so the external-IdP OIDC client secret can be stored AES-256-GCM-encrypted at rest (secretcrypt, tag encv1:) instead of plaintext — base64 ciphertext exceeds VARCHAR(255). identity encrypts on create/update + decrypts on read; access (multi_idp) decrypts its direct reads; oauth reads via identity.GetIdentityProvider. Prefix-aware reads keep legacy plaintext rows working and re-encrypt on Update; any never-updated rows migrate by re-saving the IdP at deploy. Idempotent; init-db.sql defines the column as TEXT.",
			UpSQL:       idpClientSecretWidenUp,
			DownSQL:     idpClientSecretWidenDown,
		},
		{
			Version:     67,
			Name:        "guac_token_widen",
			Description: "Widen guacamole_connection_pool.token to TEXT so the pooled Guacamole session token can be stored AES-256-GCM-encrypted at rest (secretcrypt, tag encv1:) instead of plaintext — base64 ciphertext exceeds VARCHAR(500). The column is write-only (in-memory pool serves reads), so the access service only encrypts on write; a DB dump yields no usable tokens. Idempotent; init-db.sql defines the column as TEXT.",
			UpSQL:       guacTokenWidenUp,
			DownSQL:     guacTokenWidenDown,
		},
		{
			Version:     68,
			Name:        "guacamole_recording_legal_hold",
			Description: "Guacamole recording legal-hold: guacamole_recording_legal_holds (FK -> guacamole_sessions ON DELETE CASCADE, UNIQUE active-hold partial index) so sweepExpiredGuacRecordings never purges a held recording. Parallels recording_legal_holds (v42, remote_support). Not RLS-belted; plain GRANT to openidx_app (exists by v53). Idempotent.",
			UpSQL:       guacRecordingLegalHoldsUp,
			DownSQL:     guacRecordingLegalHoldsDown,
		},
		{
			Version:     69,
			Name:        "governance_org_isolation",
			Description: "Add org_id + FORCE RLS to certification_campaigns, campaign_runs, abac_policies",
			UpSQL:       governanceOrgIsolationUp,
			DownSQL:     governanceOrgIsolationDown,
		},
		{
			Version:     70,
			Name:        "users_manager_id",
			Description: "Add the self-referential users.manager_id (nullable, ON DELETE SET NULL) + index that certification resolveReviewer's manager strategy already queries but no migration created — its absence made every 'manager' campaign silently fall back to the system admin. Populated from the SCIM enterprise-extension manager.value. No backfill; users is already under the v37 RLS belt (RI checks bypass RLS, so the self-FK resolves cross-GUC). Idempotent.",
			UpSQL:       usersManagerIDUp,
			DownSQL:     usersManagerIDDown,
		},
		{
			Version:     71,
			Name:        "temp_access_links_org_isolation",
			Description: "Add org_id (backfilled from the creating user, NOT NULL, FK) + index to temp_access_links (PAM vendor SSH/RDP/VNC access, created by v54 with no org_id). Its list/get/revoke handlers filtered by id only — a cross-tenant IDOR letting any authenticated user enumerate/read/revoke every org's access links. Handlers now add an org predicate + admin gate. Deliberately NOT RLS-belted: the public token-redemption path has no org context and the token is a unique secret. Idempotent.",
			UpSQL:       tempAccessOrgIsolationUp,
			DownSQL:     tempAccessOrgIsolationDown,
		},
		{
			Version:     72,
			Name:        "device_trust_requests_org_isolation",
			Description: "Add org_id (backfilled from the requesting user, NOT NULL, FK) + index to device_trust_requests (admin device-trust approval workflow, created by v39 with no org_id). Its approve/reject acted by bare id and its COUNT queries had no org filter — a cross-tenant IDOR letting an admin approve/reject another org's requests and read global pending counts. Service methods now carry an org predicate. Not RLS-belted (ExpireOldRequests sweeps across orgs). Idempotent.",
			UpSQL:       deviceTrustReqOrgIsolationUp,
			DownSQL:     deviceTrustReqOrgIsolationDown,
		},
		{
			Version:     73,
			Name:        "published_apps_org_isolation",
			Description: "Add org_id (published_apps backfilled from created_by's org, discovered_paths from its parent app; orphans -> oldest org; NOT NULL, FK ON DELETE CASCADE) + indexes to published_apps and discovered_paths (access App-Publish tables, created by v40 with no org_id). The app-publish handlers acted on rows by bare id with no org filter — a cross-tenant IDOR letting any caller list/read/delete another org's apps and paths. Handlers now carry an org predicate. Not RLS-belted (runAppDiscovery runs in a background goroutine with no org ctx; the integrity doctor sweeps all orgs under bypass-RLS). Idempotent.",
			UpSQL:       publishedAppsOrgIsolationUp,
			DownSQL:     publishedAppsOrgIsolationDown,
		},
		{
			Version:     74,
			Name:        "detailed_compliance_reports",
			Description: "Create the detailed_compliance_reports table (org-scoped: org_id NOT NULL, FK ON DELETE CASCADE, indexes). internal/audit's storeDetailedReport persisted every generated SOC 2 / ISO 27001 detailed report into this table, but no migration created it — every INSERT silently failed and GET /reports/:id/evidence always degraded to the basic compliance_reports fallback, so control-level evidence was never downloadable. The audit queries now carry an explicit org predicate (read is scoped by id AND org_id, closing the would-be cross-tenant evidence fetch). Not RLS-belted, matching v72/v73 precedent. Idempotent.",
			UpSQL:       detailedComplianceReportsUp,
			DownSQL:     detailedComplianceReportsDown,
		},
		{
			Version:     75,
			Name:        "known_devices_device_type",
			Description: "Add device_type VARCHAR(50) to known_devices. isCorporateDevice (device-trust auto-approval) filtered on this column, which v19 never created: the query errored on every call, the error was swallowed, and the check always returned false — the auto_approve_corporate_devices org setting (v39) was silently inert. With the column present the name-based corporate match works with existing rows and device_type='corporate' can be set by MDM/admin tooling. Idempotent.",
			UpSQL:       knownDevicesDeviceTypeUp,
			DownSQL:     knownDevicesDeviceTypeDown,
		},
		{
			Version:     76,
			Name:        "known_devices_seen_count",
			Description: "Add seen_count INTEGER NOT NULL DEFAULT 1 to known_devices and retenant portal-registered rows that fell into the primary org by column DEFAULT while their owner belongs to another org. The risk engine's device fingerprinting read/wrote seen_count plus first_seen/last_seen columns v19 never created — the new-device INSERT failed on every call, so risk device tracking never recorded a device and trust levels never progressed. Code now uses created_at/last_seen_at for the timestamps; seen_count is the one column with no existing equivalent. The retenant UPDATE corrects data mis-tenanted by the pre-fix portal RegisterDevice (fix shipped separately) and is not reversed on down. Idempotent.",
			UpSQL:       knownDevicesSeenCountUp,
			DownSQL:     knownDevicesSeenCountDown,
		},
		{
			Version:     77,
			Name:        "session_risk_history",
			Description: "Create session_risks and risk_factors for the routed continuous-auth endpoints (internal/admin/continuous_auth.go). Both tables were read and written on every risk calculation but existed in no runner migration: the previous-risk lookup and history INSERT silently failed (errors discarded), so risk deltas were always computed against 0, no history was stored, and GetRiskFactors errored outright. Org-belted (org_id NOT NULL FK) like stepup_challenges. Idempotent.",
			UpSQL:       sessionRiskHistoryUp,
			DownSQL:     sessionRiskHistoryDown,
		},
		{
			Version:     78,
			Name:        "recording_retention_policies",
			Description: "Create recording_retention_policies for the live per-org remote-support recording retention surface (internal/access/remote_support_retention.go). The GET/PUT /recording-retention-policy routes are mounted by RegisterRetentionAdminRoutes and the background retention enforcer consults the table on every sweep, but no runner migration created it: both routes 500'd and resolveEffectiveRetention silently fell through to the global default, so per-org retention was configurable in name only. org_id PRIMARY KEY (handlers upsert ON CONFLICT (org_id)); retention_days 0 = infinite per the handler contract, CHECK >= 0. Idempotent.",
			UpSQL:       recordingRetentionPoliciesUp,
			DownSQL:     recordingRetentionPoliciesDown,
		},
		{
			Version:     79,
			Name:        "oauth_signing_keys",
			Description: "Create oauth_signing_keys for DB-backed signing key rotation (internal/signingkeys). The OAuth service previously held a single RSA key in system_settings hardcoded to kid openidx-key-1, so rotation was impossible without invalidating every outstanding token. One active key (enforced by a partial unique index, which also gives EnsureActive a race-safe ON CONFLICT target) plus retired keys servable from JWKS until not_after. The legacy system_settings key is imported under its original kid on first boot so pre-upgrade tokens keep verifying. Install-wide (no org_id) like system_settings; private_key_pem encrypted at rest via ENCRYPTION_KEY. Idempotent.",
			UpSQL:       oauthSigningKeysUp,
			DownSQL:     oauthSigningKeysDown,
		},
		{
			Version:     80,
			Name:        "enrolled_agent_device_link",
			Description: "Add enrolled_agents.known_device_id FK to converge the two per-user device registries. known_devices (IAM device-trust: fingerprints + trusted flag) and enrolled_agents (Ziti endpoint agents with posture/compliance) were disjoint, so device trust and device compliance could never be reconciled for one physical device. The user-bound (OAuth) agent-enrollment path now upserts+links a known_devices row; token-enrolled and legacy agents keep known_device_id NULL. ON DELETE SET NULL. Idempotent.",
			UpSQL:       enrolledAgentDeviceLinkUp,
			DownSQL:     enrolledAgentDeviceLinkDown,
		},
		{
			Version:     81,
			Name:        "pam_connection_manager",
			Description: "PAM connection manager (Devolutions RDM parity): pam_folders (hierarchical tree), pam_entries (typed entries — rdp/ssh/vnc/telnet/website sessions, credentials, ssh keys, api keys, secure information records — with secret payloads envelope-encrypted in vault_secrets and an optional linked credential entry), pam_entry_grants (view/connect/edit/reveal ACL), pam_entry_favorites, pam_entry_access_requests (pre-connect approval), pam_entry_sessions (launch ledger). Sessions launch through the Guacamole broker with the credential injected server-side so users connect without ever seeing the password. All six tables org-scoped under the v37 FORCE-RLS belt. Idempotent.",
			UpSQL:       pamEntriesUp,
			DownSQL:     pamEntriesDown,
		},
		{
			Version:     82,
			Name:        "pam_entry_reach_mode",
			Description: "Add pam_entries.reach_mode (direct|ziti), ziti_service_name, ziti_intercept_port so a brokered PAM session can reach its target over the OpenZiti overlay (guacd → loopback → ziti-tunnel → edge-router-hosted host.v1 → target) instead of guacd dialing the target directly — removing inbound target exposure. Additive/idempotent; existing entries default to 'direct'. Partial unique index on (org_id, ziti_intercept_port) prevents broker loopback-port collisions.",
			UpSQL:       pamReachModeUp,
			DownSQL:     pamReachModeDown,
		},
		{
			Version:     83,
			Name:        "saml_sp_schema_reconcile",
			Description: "Reconcile saml_service_providers to the columns internal/oauth/saml_sp.go reads/writes (description, metadata_url, want_assertions_signed, encryption_enabled, last_used_at) — the SP list/create/update paths were erroring with 42703 (column does not exist). Additive/idempotent; TEXT cols NOT NULL DEFAULT '', booleans default false, last_used_at nullable.",
			UpSQL:       samlSPReconcileUp,
			DownSQL:     samlSPReconcileDown,
		},
		{
			Version:     84,
			Name:        "seed_mobile_oauth_client",
			Description: "Seed the openidx-mobile public (PKCE) OAuth client with the native redirect openidx://oauth-callback and refresh tokens, so the companion mobile app's login works on every install. Idempotent (ON CONFLICT DO NOTHING).",
			UpSQL:       mobileClientUp,
			DownSQL:     mobileClientDown,
		},
		{
			Version:     85,
			Name:        "seed_desktop_oauth_client",
			Description: "Seed the openidx-desktop public (PKCE) OAuth client with a fixed loopback redirect (http://127.0.0.1:47600/callback) so the Windows client's SSO works on every install. Idempotent (ON CONFLICT DO NOTHING).",
			UpSQL:       desktopClientUp,
			DownSQL:     desktopClientDown,
		},
		{
			Version:     86,
			Name:        "reusable_enrollment_tokens",
			Description: "Add agent_enrollment_tokens.reusable so a single bootstrap token can enroll a whole fleet (MDM/GPO/Intune) without being consumed. Enrollment validation skips the single-use check for reusable tokens. Additive/idempotent.",
			UpSQL:       reusableTokensUp,
			DownSQL:     reusableTokensDown,
		},
		{
			Version:     87,
			Name:        "guacamole_per_user_identities",
			Description: "Per-user non-admin Guacamole accounts (guacamole_users) + pam_entry_sessions.guac_username so PAM sessions broker under a per-user identity scoped to a single connection instead of the shared admin token. Additive/idempotent.",
			UpSQL:       guacPerUserUp,
			DownSQL:     guacPerUserDown,
		},
		{
			Version:     88,
			Name:        "audit_es_reconcile_watermark",
			Description: "Add audit_events.indexed_at (+ partial index) so a background reconciler can backfill any audit event the fire-and-forget Elasticsearch write missed, guaranteeing ES search completeness. Additive/idempotent.",
			UpSQL:       auditIndexedAtUp,
			DownSQL:     auditIndexedAtDown,
		},
		{
			Version:     89,
			Name:        "oauth_user_consents",
			Description: "Add oauth_user_consents so application_sso_settings.require_consent is enforceable: the authorize flow records a user's scope grant per client and re-prompts only when requested scopes exceed the stored grant. Org-scoped under FORCE RLS. Additive/idempotent.",
			UpSQL:       oauthUserConsentsUp,
			DownSQL:     oauthUserConsentsDown,
		},
		{
			Version:     90,
			Name:        "pam_entries_renderer",
			Description: "Add pam_entries.renderer (guacamole|wasm-ssh|novnc|support) so a PAM entry can declare which clientless in-browser renderer opens it; backfill existing rows to 'guacamole' (no behavior change). The permission/approval gate is unchanged and applies to every renderer. Additive/idempotent.",
			UpSQL:       pamEntryRendererUp,
			DownSQL:     pamEntryRendererDown,
		},
		{
			Version:     91,
			Name:        "quick_links",
			Description: "Add quick_links: an admin-curated, user-searchable launcher for support/collaboration systems. type='external' opens a URL; type='pam' references a pam_entries row and launches it clientlessly via the entry's renderer (guacamole/wasm-ssh) under the existing PAM permission gate. Org-scoped + forced RLS; role-gated by min_role. Additive/idempotent.",
			UpSQL:       quickLinksUp,
			DownSQL:     quickLinksDown,
		},
		{
			Version:     92,
			Name:        "remote_support_consent",
			Description: "Add remote_support_sessions.consent_required/consent_status/consent_decided_at so an attended support session can require the person at the device to click Allow before the admin can view/control. Existing sessions default consent_required=false (no behavior change for unattended/server support). Additive/idempotent.",
			UpSQL:       remoteSupportConsentUp,
			DownSQL:     remoteSupportConsentDown,
		},
		{
			Version:     93,
			Name:        "device_fingerprint",
			Description: "Add enrolled_agents.device_fingerprint + a partial unique index so re-enrollment of the same physical device reuses one agent_id/auth_token instead of minting a new row every install. Existing rows keep fingerprint NULL and are unaffected. Additive/idempotent.",
			UpSQL:       deviceFingerprintUp,
			DownSQL:     deviceFingerprintDown,
		},
		{
			Version:     94,
			Name:        "remote_support_transport",
			Description: "Add remote_support_sessions.transport (webrtc|relay) so a session can use a fully server-relayed VP8-over-WebSocket media path instead of peer-to-peer WebRTC. Defaults to 'webrtc' (no behavior change). Enables full-Ziti, no-STUN remote support.",
			UpSQL:       remoteSupportTransportUp,
			DownSQL:     remoteSupportTransportDown,
		},
		{
			Version:     95,
			Name:        "outbound_scim",
			Description: "Add scim_target_apps + scim_provisioning_records + scim_provisioning_queue so OpenIDX can act as a SCIM 2.0 client and provision users/groups OUT to downstream SaaS (Okta/Entra/Slack/...). Outbox-based at-least-once delivery with retry/backoff. Org-scoped for RLS. Additive/idempotent; no behavior change until a target app is configured.",
			UpSQL:       outboundScimUp,
			DownSQL:     outboundScimDown,
		},
		{
			Version:     96,
			Name:        "hr_jml",
			Description: "Add HR/JML user attributes (employee_number, job_title, department, hire_date, termination_date, employment_status, external_hr_id) + supporting indexes so an HRIS source (BambooHR/Workday/SuccessFactors, modeled as a directory-connector type) can drive joiner/mover/leaver. Additive/idempotent; NULL for existing users.",
			UpSQL:       hrJmlUp,
			DownSQL:     hrJmlDown,
		},
		{
			Version:     97,
			Name:        "dcr_registration_tokens",
			Description: "Add oauth_registration_tokens (hashed RFC 7592 registration access token per dynamically-registered client) so an RFC 7591-registered client can manage its own credential lifecycle. Token Exchange (RFC 8693) needs no schema. Additive/idempotent.",
			UpSQL:       dcrTokenExchangeUp,
			DownSQL:     dcrTokenExchangeDown,
		},
		{
			Version:     98,
			Name:        "edr_posture_ingestion",
			Description: "Add edr_posture_sources + edr_device_mappings so an external EDR/MDM (CrowdStrike/Intune/Jamf) can feed the existing Ziti-bound posture pipeline: a non-compliant device signal writes a failing device_posture_results row, so the proxy/continuous-verify enforcement revokes the session and severs the overlay circuit automatically. Org-scoped, encrypted creds. Additive/idempotent.",
			UpSQL:       edrPostureUp,
			DownSQL:     edrPostureDown,
		},
		{
			Version:     99,
			Name:        "ssf_caep",
			Description: "Add ssf_streams + ssf_stream_delivery (outbox) + ssf_received_events so OpenIDX is both an SSF/CAEP transmitter (push signed SETs on session-revoked etc.) and receiver (apply inbound SETs, with Ziti session termination as the actuator). Org-scoped, encrypted delivery auth. Additive/idempotent.",
			UpSQL:       ssfCaepUp,
			DownSQL:     ssfCaepDown,
		},
		{
			Version:     100,
			Name:        "network_revocation_queue",
			Description: "Add network_revocation_queue so the governance service can hand a circuit-severance intent to the access-service (which owns the ZitiManager) on an access-review/certification revoke or JIT expiry. A worker terminates the subject's live overlay circuits, completing decision-to-packet. Additive/idempotent.",
			UpSQL:       networkRevocationQueueUp,
			DownSQL:     networkRevocationQueueDown,
		},
		{
			Version:     101,
			Name:        "network_grant_queue",
			Description: "Add network_grant_queue (+ network_revocation_queue.attribute) for JIT network grants (Wave B1): fulfilling a network_service access request adds a time-bound Ziti role attribute (jit-<request-id>) that opens the dial; expiry removes it and severs the circuit. Governance->access handoff. Additive/idempotent.",
			UpSQL:       networkGrantQueueUp,
			DownSQL:     networkGrantQueueDown,
		},
		{
			Version:     102,
			Name:        "usage_metering",
			Description: "Add usage_metering_daily + usage_metering_cursor (Wave A4): a daily rollup of Ziti fabric usage (overlay logins, service dials) from unified_audit_events, per org/service/identity. The MSP billing substrate. Additive/idempotent.",
			UpSQL:       usageMeteringUp,
			DownSQL:     usageMeteringDown,
		},
		{
			Version:     103,
			Name:        "mcp_gateway",
			Description: "Add mcp_servers + mcp_tool_policies (Wave D1): the MCP/AI-agent gateway. An agent authenticates with an OpenIDX token, a per-tool allowlist gates the call, and the request forwards to the MCP server (published as a dark Ziti service). Network-enforced agent containment. Org-scoped. Additive/idempotent.",
			UpSQL:       mcpGatewayUp,
			DownSQL:     mcpGatewayDown,
		},
		{
			Version:     104,
			Name:        "groups_per_org_name",
			Description: "Replace the GLOBAL unique constraint on groups.name with a composite UNIQUE (org_id, name) (Wave A2): two tenants may now both own an 'Engineering'/'Admins' group, the prerequisite for per-org overlay attribute scoping (ZitiPerOrgAttributes). Idempotent; down is best-effort and only restores the global constraint when names are still globally unique.",
			UpSQL:       groupsPerOrgNameUp,
			DownSQL:     groupsPerOrgNameDown,
		},
		{
			Version:     105,
			Name:        "pam_checkout_controls",
			Description: "Add PAM checkout controls: break_glass_enabled/dual_control_required/exclusive_checkout flags on pam_entries plus pam_checkout_authorizations (two-person second-approver ledger) and pam_active_checkouts (live exclusivity/break-glass ledger). Privileged-access two-person rule + exclusivity + audited emergency access (DORA RTS Art 21). Additive/idempotent.",
			UpSQL:       pamCheckoutControlsUp,
			DownSQL:     pamCheckoutControlsDown,
		},
		{
			Version:     106,
			Name:        "sod_violations",
			Description: "Add sod_violations — the detective segregation-of-duties register. A periodic sweep scans existing user role/group assignments against separation_of_duty policies and records who currently holds a conflicting combination (vs the preventive control that only blocks new grants). Tracks each violation to closure (open/acknowledged/resolved/waived). SOX/ISAE/DORA detective control. Org-scoped, additive/idempotent.",
			UpSQL:       sodViolationsUp,
			DownSQL:     sodViolationsDown,
		},
		{
			Version:     107,
			Name:        "privileged_accounts_discovered",
			Description: "Add privileged_accounts_discovered — the privileged-account discovery register. A periodic scan sweeps the identity store for the privileged population (standing/non-expiring admin grants, privileged group membership, service-account naming patterns, dormant privileged accounts) and tracks each to onboarding/acceptance. The discovery substrate for PAM/JIT onboarding. Org-scoped, additive/idempotent.",
			UpSQL:       privilegedAccountsUp,
			DownSQL:     privilegedAccountsDown,
		},
		{
			Version:     108,
			Name:        "entitlement_warehouse",
			Description: "Add entitlement_warehouse — a denormalized snapshot unioning every entitlement source (user_roles, group_memberships, pam_entry_grants, vault_access_grants) into one 'who has access to what' surface, each row stamped time_bound + orphaned (owner disabled/missing). The access-certification and orphan-detection substrate. Org-scoped, additive/idempotent.",
			UpSQL:       entitlementWarehouseUp,
			DownSQL:     entitlementWarehouseDown,
		},
		{
			Version:     109,
			Name:        "ssh_ca_session_broker",
			Description: "Add ssh_ca (per-org SSH certificate authority; private key held in the vault, only the public key stored) + brokered_sessions (the ssh/db/k8s brokered-connection ledger). The `openidx connect` substrate: short-lived, principal-scoped SSH certs instead of standing keys (Wave C1). Org-scoped, additive/idempotent.",
			UpSQL:       sshCASessionBrokerUp,
			DownSQL:     sshCASessionBrokerDown,
		},
		{
			Version:     110,
			Name:        "ziti_ai_insights",
			Description: "Add ziti_identity_activity (learned per-identity behavioral baselines: session counts + hour-of-day histograms per service), ziti_ai_anomalies (deviation ledger: new-service access, off-hours activity, dormant reactivation, session spikes) and ziti_ai_quarantine (saved pre-quarantine role attributes for restore). The AI-driven network intelligence substrate for the Ziti overlay. Controller-scoped like other ziti_* tables, additive/idempotent.",
			UpSQL:       zitiAIInsightsUp,
			DownSQL:     zitiAIInsightsDown,
		},
		{
			Version:     111,
			Name:        "posture_check_platforms",
			Description: "Add posture_checks.platforms JSONB (+GIN index) so the agent config endpoint can filter posture checks to the reporting device's platform (android/ios/any). The column was already referenced by internal/access/agent_api.go HandleConfig but never created, breaking mobile posture with SQLSTATE 42703. NULL means all-platforms; additive/idempotent.",
			UpSQL:       postureCheckPlatformsUp,
			DownSQL:     postureCheckPlatformsDown,
		},
		{
			Version:     112,
			Name:        "remote_support_recording_purged_at",
			Description: "Add remote_support_sessions.recording_purged_at (+partial index) so the remote-support recording retention sweeper can track purged blobs. The column was referenced by internal/access/remote_support_retention.go but never added to this table (only to guacamole_sessions in v59), so the sweeper failed every run with SQLSTATE 42703 and expired recordings were never purged. Additive/idempotent.",
			UpSQL:       remoteSupportPurgedAtUp,
			DownSQL:     remoteSupportPurgedAtDown,
		},
		{
			Version:     113,
			Name:        "roles_updated_at",
			Description: "Add roles.updated_at (backfilled from created_at). CreateRole/UpdateRole in internal/identity/service.go both write updated_at, but the roles table (seeded in v10) only had created_at, so every role create/edit failed with SQLSTATE 42703 and the Roles admin page could not create or update a role. Additive/idempotent.",
			UpSQL:       rolesUpdatedAtUp,
			DownSQL:     rolesUpdatedAtDown,
		},
		{
			Version:     114,
			Name:        "audit_webhook_subscriptions",
			Description: "Add audit_webhook_subscriptions for the event-streamer's outbound webhooks (route /api/v1/audit/webhooks). internal/audit and internal/webhooks both used a table named webhook_subscriptions with incompatible schemas; the admin webhooks migration won, so every audit-webhook query failed with SQLSTATE 42703. This gives the audit streamer its own table (enabled/filters/last_delivery/failure_count) and the audit code targets it. Admin webhook_subscriptions left untouched. Additive/idempotent.",
			UpSQL:       auditWebhookSubscriptionsUp,
			DownSQL:     auditWebhookSubscriptionsDown,
		},
		{
			Version:     115,
			Name:        "vault_require_step_up",
			Description: "Add vault_secrets.require_step_up so PAM can enforce step-up MFA at credential reveal for high-value secrets. The step-up challenge/verify flow already records completed challenges, but nothing on the reveal path required one; a valid grant alone revealed the secret. internal/vault Reveal now refuses (403, X-Step-Up-Required) unless the caller completed a step-up within the recent window. Additive, defaults false.",
			UpSQL:       vaultRequireStepUpUp,
			DownSQL:     vaultRequireStepUpDown,
		},
		{
			Version:     116,
			Name:        "guac_recording_seal",
			Description: "Add guacamole_sessions recording-seal metadata (recording_sealed_at, recording_sha256, recording_key_id) plus a partial index. guacd writes RDP/SSH recordings to disk as plaintext (openidx never sees those bytes inline, unlike the already-encrypted WebRTC remote-support path), so a filesystem compromise leaks a replayable recording. PAM A1 adds a leader-gated sealer that encrypts each finished recording through the existing AES-256-GCM keyring and stores an integrity hash. Additive/nullable — NULL sealed_at means still plaintext (sealer off or not yet run).",
			UpSQL:       guacRecordingSealUp,
			DownSQL:     guacRecordingSealDown,
		},
		{
			Version:     117,
			Name:        "moderated_sessions",
			Description: "Add moderated privileged sessions (PAM C3): guacamole_connections.require_moderator flag + guacamole_moderation_sessions state machine (pending→active→ended/expired). Teleport-style four-eyes control — a flagged session does not start until a second authorized user (moderator) joins to watch it live, distinct from require_approval which is decided before the session. New table is org-scoped (FORCE RLS, bypass_rls-aware policy) with hot-path partial index on pending rows. Additive; require_moderator defaults false so existing connections are unchanged.",
			UpSQL:       moderatedSessionsUp,
			DownSQL:     moderatedSessionsDown,
		},
		{
			Version:     118,
			Name:        "mcp_tool_approval",
			Description: "Add AI-agent tool-call approval (PAM C5): mcp_tool_policies.require_approval flag + mcp_tool_approvals HITL ledger (pending→approved/denied/expired/consumed). The MCP gateway pauses a require_approval tool invocation until a human approves it, binding the approval to the exact call via a sha256 request hash. New table is org-scoped (FORCE RLS, bypass-aware policy) with a hot-path partial index on pending rows. Additive; require_approval defaults false so existing allowlist entries invoke immediately as before.",
			UpSQL:       mcpToolApprovalUp,
			DownSQL:     mcpToolApprovalDown,
		},
		{
			Version:     119,
			Name:        "windows_apps",
			Description: "Windows application delivery — app catalog + host pools on top of the RemoteApp launch path. An app host stays a pam_entries row (new entry_type windows_app_host) so it inherits vault injection, Ziti reach, recording, approval and checkout unchanged; four new org-scoped (FORCE RLS) tables add the app layer: windows_app_pools (interchangeable host set + placement strategy), windows_app_pool_members (membership + per-host max_sessions capturing RDS-vs-desktop concurrency), windows_apps (a published app bound to exactly one host OR one pool via a CHECK, with source/verified_at distinguishing published-and-launchable from installed-only and nullable require_approval/record_session per-app tightening overrides), and windows_app_host_state (posture snapshot flagging the dangerous fAllowUnlistedRemotePrograms). Additive; existing RDP entries with remote-app settings keep working.",
			UpSQL:       windowsAppsUp,
			DownSQL:     windowsAppsDown,
		},
		{
			Version:     120,
			Name:        "agent_windows_app_host",
			Description: "Bind an enrolled agent to a windows_app_host pam_entry (nullable windows_app_host_entry_id FK on enrolled_agents, ON DELETE SET NULL) so agent-based discovery can route a posted discoveryReport to the right host and tenant. enrolled_agents is a global fleet table; the org is derived from the linked entry. Additive — agents stay unlinked until an admin binds them.",
			UpSQL:       agentWindowsAppHostUp,
			DownSQL:     agentWindowsAppHostDown,
		},
		{
			Version:     121,
			Name:        "rls_belt_gap",
			Description: "Close the FORCE-RLS belt gaps. Six org-scoped tables were added after the v37/v81/v94 belt without a row-level-security policy: ssf_streams, ssf_stream_delivery, ssf_received_events (v99 — its own comment claims they are org-scoped for RLS, but no policy was created), plus session_risks, risk_factors (v77) and recording_retention_policies (v78). Each now gets the standard pol_<table>_org_scope policy keyed on the app.org_id GUC with ENABLE + FORCE, so a missing org predicate in application code can no longer leak across tenants. Ships with the SSF route/query fixes that this backstops.",
			UpSQL:       rlsBeltGapUp,
			DownSQL:     rlsBeltGapDown,
		},
		{
			Version:     122,
			Name:        "refresh_token_reuse_detection",
			Description: "Add family_id, used_at and revoked_at to oauth_refresh_tokens so rotation can tombstone the old token instead of deleting it. A hard DELETE made a replayed refresh token indistinguishable from a bogus one and left the newest token working, so refresh-token theft produced a single invalid_grant and no revocation. With a family and a tombstone, presenting an already-rotated token proves two parties hold the same secret and revokes the whole chain (RFC 6819 §5.2.2.3 / OAuth 2.1 §6.1). Additive and backfilled — pre-existing tokens become single-member families and keep working.",
			UpSQL:       refreshTokenReuseUp,
			DownSQL:     refreshTokenReuseDown,
		},
		{
			Version:     123,
			Name:        "totp_verification_throttle",
			Description: "Add failed_attempts, last_failed_at and locked_until to mfa_totp so TOTP verification can be rate limited. VerifyTOTP had no attempt cap: a six-digit code with the verifier's +/-1 step skew is roughly 3 in 10^6 per guess, so an unbounded endpoint yields a hit within minutes of sustained traffic. State is stored here rather than in Redis because Redis is optional in this deployment and a brute-force counter that stops counting during an outage is not a control. Columns mirror the account-lockout fields on users so there is one lockout pattern rather than two. Additive and defaulted; existing rows read as no failures.",
			UpSQL:       totpThrottleUp,
			DownSQL:     totpThrottleDown,
		},
		{
			Version:     124,
			Name:        "ziti_admin_password_secretcrypt_tag",
			Description: "Tag the stored Ziti controller admin password as secretcrypt \"encv1:\" so it moves onto the shared secret facility instead of the private AES-256-GCM helper in internal/access. The two formats are byte-identical apart from the tag — base64(nonce||ciphertext), AES-256-GCM, no AAD, same ENCRYPTION_KEY — so this is a keyless re-tag, not a re-encryption. The tag is what makes the move safe: secretcrypt passes an untagged value through Decrypt unchanged so plaintext survives a rollout, so an untagged Ziti ciphertext would otherwise have come back as the base64 blob itself and been sent to a controller login as the password, with no error anywhere. Idempotent; rows already tagged are skipped. Reads accept both shapes, so this converges the data rather than gating correctness. Tagging is also what puts the password inside the rotation sweep: cmd/rekey re-seals tagged strings found anywhere in a json/jsonb document, so this value — which lives inside system_settings.value — is carried forward by a key retirement instead of being stranded under a key the operator believes was retired.",
			UpSQL:       zitiAdminPwTagUp,
			DownSQL:     zitiAdminPwTagDown,
		},
		{
			Version:     125,
			Name:        "oauth_device_codes",
			Description: "Add oauth_device_codes for the RFC 8628 device authorization grant, so input-constrained clients (TV apps, headless CLIs, kiosks) can authorize against a second device instead of being pushed onto a password grant or a copy-pasted token. Stores the device_code only as a SHA-256 hash — it is a bearer credential the client polls with, and a database disclosure should not hand the reader a set of redeemable grants — while user_code is normalized plaintext because the verification page has to look it up from what a human typed; its defence is entropy plus a short lifetime plus throttling, not storage. The unique index on (org_id, user_code) is what makes that lookup unambiguous and deliberately covers expired rows, so a code is never reissued while its previous owner may still be reading it off a screen.",
			UpSQL:       deviceCodesUp,
			DownSQL:     deviceCodesDown,
		},
		{
			Version:     126,
			Name:        "device_trust_settings_unique_org",
			Description: "Add a unique index on device_trust_settings.org_id so UpdateDeviceTrustSettings can upsert (ON CONFLICT (org_id)). The table is a per-org singleton but only had a PK on id; on a fresh install with no settings row the old UPDATE ... WHERE id=? affected zero rows, so an admin enabling auto-approve-known-IPs (etc.) never persisted, leaving clientless BrowZer logins stuck behind manual device approval. Collapses any accidental duplicate org rows (keeping the newest) before creating the index.",
			UpSQL:       deviceTrustSettingsUniqueOrgUp,
			DownSQL:     deviceTrustSettingsUniqueOrgDown,
		},
		{
			Version:     127,
			Name:        "social_providers_nullable_provider_id",
			Description: "Drop NOT NULL on social_providers.provider_id. The column is a FK to identity_providers, but the admin console's create-social-provider flow only supplies a provider_key (google/github/...) and no identity_providers UUID, so every create failed with 500 'invalid input syntax for type uuid' (22P02). Nullable provider_id lets an admin register a social-login button and link it to a configured identity provider later.",
			UpSQL:       socialProvidersNullableProviderIDUp,
			DownSQL:     socialProvidersNullableProviderIDDown,
		},
		{
			Version:     128,
			Name:        "normalize_agent_compliance_score",
			Description: "Clamp enrolled_agents.compliance_score into its documented 0.0-1.0 range. Some agents reported an already-percentage 0-100 value that was stored verbatim; the console multiplies by 100 to render a percent, so a stored 100 showed as '10000%'. Values in (1,100] are divided by 100, >100 clamped to 1.0, negatives floored at 0.0. The write path now clamps too.",
			UpSQL:       normalizeAgentComplianceScoreUp,
			DownSQL:     normalizeAgentComplianceScoreDown,
		},
		{
			Version:     129,
			Name:        "seed_email_templates",
			Description: "Seed the standard transactional email templates (magic link, invitation, password reset, email verification, MFA enrollment, welcome). The Email Templates admin page had no create endpoint and the table was never seeded, so it was permanently empty and the edit workflow was untestable (QA 10.4). Idempotent via ON CONFLICT (slug) DO NOTHING.",
			UpSQL:       seedEmailTemplatesUp,
			DownSQL:     seedEmailTemplatesDown,
		},
		{
			Version:     130,
			Name:        "upstream_pools",
			Description: "Add upstream_pools + upstream_pool_members and a nullable proxy_routes.upstream_pool_id, so a route can point at several weighted backends with active health checking instead of the single address in to_url. Measured before this change: 0 of 19 edge routes had more than one node and none had a health check, so a dead backend took its route down with it. The link column is NULLABLE and defaults to NULL, meaning 'keep using to_url exactly as before', so no existing route changes behaviour. RLS mirrors proxy_routes (org-scoped, bypassable by the reconciler).",
			UpSQL:       upstreamPoolsUp,
			DownSQL:     upstreamPoolsDown,
		},
		{
			Version:     131,
			Name:        "seed_playground_client",
			Description: "Register the 'playground-client' OAuth public client that the OAuth Playground (/oauth-playground) defaults to. It was never seeded, so Step 2 Authorize failed with invalid_client and Steps 3-4 were untestable (QA 11.2). Public PKCE client, no secret; redirect_uris cover the SPA dev origin, admin-api dev origin, and the production site. Idempotent via ON CONFLICT (id) DO NOTHING.",
			UpSQL:       seedPlaygroundClientUp,
			DownSQL:     seedPlaygroundClientDown,
		},
		{
			Version:     132,
			Name:        "enrollment_sessions",
			Description: "Add enrollment_sessions, the unified self-service onboarding primitive: a short code + QR + deep-link created from an MFA-verified console session that maps onto the existing single-use agent_enrollment_tokens flow (correlated by token_hash). Carries the creating user + org + a server-verified mfa_verified flag that drives risk-based device auto-trust. Global agent-infrastructure table (no RLS) like agent_enrollment_tokens, since the public /agent/enroll path looks it up by high-entropy token_hash without a tenant JWT.",
			UpSQL:       enrollmentSessionsUp,
			DownSQL:     enrollmentSessionsDown,
		},
		{
			Version:     133,
			Name:        "sessions_auth_methods",
			Description: "Add sessions.auth_methods TEXT[] to record the authentication methods used at login (pwd, plus mfa when a second factor was verified), so OAuth tokens minted from the session can emit an `amr` claim. This activates the device auto-trust from v132/DEVICE_AUTOTRUST, which keys on a server-verified MFA signal.",
			UpSQL:       sessionsAuthMethodsUp,
			DownSQL:     sessionsAuthMethodsDown,
		},
		{
			Version:     134,
			Name:        "selfheal_manage_permission",
			Description: "Seed the selfheal:manage permission and grant it to admin/super_admin roles across all orgs. The self-heal control panel gates its mutating endpoints (mode/kill-switch/sweep) on this permission, which PermissionResolver reads from role_permissions in the DB (not the in-code RBAC maps), so admins would be 403'd on control actions without it.",
			UpSQL:       selfHealPermUp,
			DownSQL:     selfHealPermDown,
		},
		{
			Version:     135,
			Name:        "push_device_agent_link",
			Description: "Link mfa_push_devices to the enrolled agent that auto-registered it (FastPass convergence): nullable agent_id/device_id/enrollment_session_id + an agent_id index. Set when a device enrollment mints a push-enrollment ticket so the phone self-registers as a push approver in one step; the existing QR self-enroll path leaves them NULL. agent_id is the de-dup key across FCM-token rotation and the join for login-time device preference.",
			UpSQL:       pushDeviceAgentLinkUp,
			DownSQL:     pushDeviceAgentLinkDown,
		},
		{
			Version:     136,
			Name:        "group_application_assignments",
			Description: "Add group_application_assignments (group→app) so SSO/OIDC app access can be granted to a group, not just a user. GetMyApplications returns the UNION of user- and group-assigned apps. Additive; the user-assignment path is unchanged.",
			UpSQL:       groupAppAssignmentsUp,
			DownSQL:     groupAppAssignmentsDown,
		},
		{
			Version:     137,
			Name:        "application_require_assignment",
			Description: "Add applications.require_assignment (default false): opt-in gate making /oauth/authorize refuse a token for an application the caller is not assigned. Off by default so first-party clients with no assignments keep working; enforcement is staged separately behind ACCESS_ASSIGNMENT_ENFORCE.",
			UpSQL:       applicationRequireAssignmentUp,
			DownSQL:     applicationRequireAssignmentDown,
		},
		{
			Version:     138,
			Name:        "ispm_ai_org_isolation",
			Description: "Add org_id + FORCE RLS to ispm_rules/ispm_findings/ispm_scores (v54), ai_agents + ai_agent_credentials/permissions/activity (v42/v43) and ai_recommendations + recommendation_history (v43). They had no org_id and internal/admin read/wrote them by bare id — any tenant admin could read, dismiss, remediate and delete every other tenant's posture findings, disable every tenant's posture rules, and enumerate/modify/delete every tenant's AI agents and recommendations from the console. Install-wide unique keys become per-tenant: ispm_rules(org_id, check_type), ai_agents(org_id, name), ispm_scores(org_id, snapshot_date) — the last one also stops one tenant's daily posture score overwriting another's. Pre-existing rows are attributed to the oldest org (children follow their parent); no column DEFAULT, so a rolled-back binary fails loudly instead of mis-tenanting.",
			UpSQL:       ispmAIOrgIsolationUp,
			DownSQL:     ispmAIOrgIsolationDown,
		},
		{
			Version:     139,
			Name:        "hardware_token_throttle",
			Description: "Add failed_attempts/last_failed_at/locked_until to hardware_tokens, mirroring mfa_totp. VerifyHardwareToken checked a six-digit code with no failure counter and no lockout, and its HOTP look-ahead of ten counters plus the TOTP +/-1 step window leave roughly eleven of a million values live at any instant — a second factor is only a factor if guessing it is expensive. Additive and idempotent; hardware_tokens still has no org_id (orgscope needsScoping register, its own migration).",
			UpSQL:       hardwareTokenThrottleUp,
			DownSQL:     hardwareTokenThrottleDown,
		},
		{
			Version:     140,
			Name:        "rls_belt_batch_1",
			Description: "Extend the FORCE-RLS belt to fifteen org-scoped tables that drifted out of it: scheduled_reports, detailed_compliance_reports, audit_webhook_subscriptions, usage_metering_daily, email_branding, device_trust_settings, pam_active_checkouts, pam_checkout_authorizations, brokered_sessions, ssh_ca, sod_violations, privileged_accounts_discovered, entitlement_warehouse, upstream_pools and upstream_pool_members. Each carries org_id and each of its queries already names it, so the belt cannot change what any query returns — it stops a future query that forgets the predicate from crossing tenants silently. scheduled_reports, device_trust_settings, email_branding and usage_metering_daily also get org_id NOT NULL (oldest org for the first three, the zero UUID for unattributable metering rows), because a NULL org under a belt is an invisible row rather than a loudly wrong one. email_branding's handlers never read the caller's org at all and are fixed in the same commit; under this policy the old write would be refused by WITH CHECK.",
			UpSQL:       rlsBeltBatch1Up,
			DownSQL:     rlsBeltBatch1Down,
		},
		{
			Version:     141,
			Name:        "audit_tenant_scope",
			Description: "Add org_id + FORCE RLS to admin_audit_log, audit_archives and audit_retention_policies. All three held one tenant's compliance record in a table with nowhere to say whose it was, and every handler read them accordingly: the admin log was listed WHERE 1=1 and fetched by bare id (one tenant's admin could read another's full administrative history, before/after JSON included), archives were listed and RESTORED by bare id (a tenant could name another tenant's export and have the product read that file back), and retention policies were listed and updated or deleted by bare id (one tenant could shorten another's retention). Rows are attributed to their own actor's org where one survives (actor_id, created_by) and to the oldest org otherwise; retention policies carry no actor, so all existing ones go to the oldest org where they stay visible and can be re-filed. No column DEFAULT, so a rolled-back binary fails loudly instead of mis-tenanting.",
			UpSQL:       auditTenantScopeUp,
			DownSQL:     auditTenantScopeDown,
		},
		{
			Version:     142,
			Name:        "unified_audit_org_scope",
			Description: "Add org_id + FORCE RLS to unified_audit_events, the table behind the console's Unified Audit page, the assignment- and ABAC-gate decision records, the agent lifecycle log, the MCP tool-call log, the Ziti/Guacamole sync and the usage metering rollup. It had no org_id at all and QueryEvents opened WHERE 1=1, so every tenant's admin read every tenant's audit trail — enforcement decisions on other tenants' applications, their users' IP addresses and (via the JOIN) e-mail addresses, and their route names; the summary endpoint counted install-wide the same way. The absence had been written down as a design property in three files, because the assignment gate chose this table over the org-scoped audit_events when audit_events was rejecting its writes — itself a bug (a detached context left app.org_id empty), fixed earlier in this series. Rows are attributed to their own user's org, else the org of the route they name (which covers the Ziti and Guacamole sync rows), else the oldest org for controller-level events that match neither. No column DEFAULT, so a rolled-back binary fails loudly instead of filing unattributed rows.",
			UpSQL:       unifiedAuditOrgScopeUp,
			DownSQL:     unifiedAuditOrgScopeDown,
		},
		{
			Version:     143,
			Name:        "signin_tenant_scope",
			Description: "Add org_id + FORCE RLS to social_providers, trusted_browsers, passwordless_preferences, user_risk_baselines and phone_call_challenges. social_providers is the live hole: its list query put the org predicate inside a LEFT JOIN's ON clause, where it filters nothing on the driving table, so every tenant's social providers were listed to every tenant with only the joined idp_name blanked out; get, update and delete then took a bare id with no org at all, and internal/oauth/social_policy.go reads this table on the login path, so one tenant could change which sign-in buttons another tenant's users see and which e-mail domains auto-provision. provider_key was UNIQUE install-wide (the v138 ispm_rules.check_type shape: the first tenant to register 'google' took it from everybody else) and becomes UNIQUE(org_id, provider_key). The other four were already keyed by the org-scoped user_id, so they are depth rather than a live hole — with two exceptions the belt closes: trusted_browsers was updated by bare id at two sites, and phone_call_challenges.user_id is nullable, so a challenge with no user sat in nobody's scope. Social providers are attributed to the org of the identity provider they extend; the per-user tables to their user, with the oldest-org fallback where user_id is nullable. No column DEFAULT.",
			UpSQL:       signinTenantScopeUp,
			DownSQL:     signinTenantScopeDown,
		},
		{
			Version:     144,
			Name:        "saml_tenant_scope",
			Description: "Add org_id + FORCE RLS to saml_service_providers and saml_sessions. saml_service_providers is the registry of federation partners this install acts as a SAML IdP for — entity id, ACS URL and the certificate used to verify their requests — and every handler read it install-wide: the list and its count had no org predicate at all, and get, update and delete took a bare id, so one tenant's admin could enumerate another tenant's partners, rewrite an ACS URL to point their assertions at a host of their choosing, or replace the certificate. saml_sessions is the SLO bookkeeping, keyed by user_id and so scoped in practice; the belt makes it structural. entity_id deliberately KEEPS its install-wide UNIQUE, unlike v143's provider_key: a SAML entity id is a globally unique URI by specification and it is what resolves the tenant on an incoming AuthnRequest, so scoping the key would make that lookup ambiguous and scoping the query would make it impossible. That lookup and the SLO session lookup run bypassed with the reason recorded, joining the api-key-by-hash and route-by-host class. Sessions are attributed to their user's org; service providers, which carry no such link, to the oldest org where an operator can re-file them. No column DEFAULT.",
			UpSQL:       samlTenantScopeUp,
			DownSQL:     samlTenantScopeDown,
		},
		{
			Version:     145,
			Name:        "credential_tenant_scope",
			Description: "Add org_id + FORCE RLS to hardware_tokens, hardware_token_events, mfa_bypass_codes, mfa_bypass_audit and magic_links — five v54 tables that are all a way to authenticate WITHOUT the password, and none of which carried a tenant. hardware_tokens is the live hole: internal/identity/hardware_token.go read and wrote it install-wide at every call site, so ListHardwareTokens showed one tenant every other tenant's inventory of physical tokens, get/revoke/report-lost took a bare id, and AssignHardwareToken took a bare token id AND a bare user id — an admin of tenant A could bind a token sitting available in tenant B's inventory to one of their own users. mfa_bypass_codes had its list scoped through the target user's org but RevokeBypassCode took a bare code id and RevokeAllBypassCodes a bare user id, so one tenant could destroy another's break-glass; GetBypassAuditLog's `WHERE ($1 = '' OR user_id::text = $1)` returned every tenant's bypass history when the console called it with no user. serial_number was UNIQUE install-wide and becomes UNIQUE(org_id, serial_number) — the opposite call from v144's entity_id, because a serial resolves no tenant (verification finds a token through assigned_to) while the install-wide constraint handed the first registrant a veto and answered existence questions about hardware another tenant owns. Per-user rows are attributed to their user; hardware_tokens rows exist before any user is attached, so unassigned ones go to the oldest org where an operator can re-file them. No column DEFAULT.",
			UpSQL:       credentialTenantScopeUp,
			DownSQL:     credentialTenantScopeDown,
		},
		{
			Version:     146,
			Name:        "mfa_factor_tenant_scope",
			Description: "Add org_id + FORCE RLS to mfa_sms, mfa_email_otp, mfa_phone_call and mfa_otp_challenges — the four second-factor tables the belt had skipped. Three of the six factors (mfa_totp, mfa_push_devices, mfa_webauthn) already carried org_id, and internal/admin/mfa_management.go listed all six in one SELECT with three carrying `AND x.org_id = $1` and three not, under a comment recording the asymmetry as a property of the schema. Every query is keyed on user_id and a user belongs to one organization, so this is depth rather than a live hole; what makes it worth doing is mfa_otp_challenges, which holds the code hash, the recipient (a real phone number or e-mail) and the requester's IP for every OTP in flight with no tenant column at all, and whose status and attempt counter are updated by bare id. It also completes a pair v143 left half-done: that migration belted phone_call_challenges without belting mfa_phone_call, the enrolment they are issued against. THE BELT ITSELF IS THE HAZARD HERE, pointing the opposite way from v145: evaluateMFA in internal/oauth/mfa_policy.go asks each enrolment table in turn, discards the error and reads nil as 'not enrolled', so an RLS-empty read on a connection with no app.org_id would make a user whose only factor is SMS sign in with no second factor at all — the belt letting everybody in rather than locking anybody out. Those policy reads therefore run bypassed with the tenant in the predicate, pinned by a test. UNIQUE(user_id) is deliberately NOT re-scoped, the third case in the taxonomy v143 and v144 built: user_id already determines org_id, so a per-org key would accept strictly more rows and the extra rows are corrupt ones. Existing rows are attributed to their user, with the oldest organization as the fallback mfa_phone_call's nullable user_id makes load-bearing. No column DEFAULT.",
			UpSQL:       mfaFactorTenantScopeUp,
			DownSQL:     mfaFactorTenantScopeDown,
		},
		{
			Version:     147,
			Name:        "breach_response_tenant_scope",
			Description: "Add org_id + FORCE RLS to breach_incidents and breach_alerts — the Identity Breach Detection and Response record of what was detected, which users and sessions it affected, and what containment was applied. Neither carried a tenant: handleIBDRIncidents, the console's incident list, was `SELECT ... FROM breach_incidents ORDER BY first_detected_at DESC LIMIT 100` with no predicate at all; GetBreachAlerts filtered on `acknowledged` and nothing else while each alert names a user_id, a session_id and an IP address; AnalyzeBreachPatterns aggregated the whole install. THE SHARPEST PART IS THE CONTAINMENT, and it is a direction the earlier batches did not cover: TriggerIncidentResponse took a bare incident id, flipped the incident to 'investigating' and recorded containment steps against it, while the actions it invokes — executeFullQuarantine and revokeUserSessions — were ALREADY org-scoped. So an administrator of one tenant triggering response on another tenant's incident disabled nobody and revoked nothing, and left the owning tenant's real incident marked as handled: a containment that reports success and contains nothing. Scoping the action without scoping the record it acts on converts a cross-tenant write into a silent no-op instead of a refusal, which is worse than either half alone; the fix scopes the record, so the trigger refuses. internal/admin/ai_intelligence.go had already written the gap down as a property — 'breach incidents are install-wide; affected_user_ids carries users.id values so scoping happens implicitly via the org's user set' — the v143 social_providers shape: implicit scoping through a joined set holds until the query changes, and nothing made it hold. Existing incidents are attributed through the first entry of affected_user_ids, alerts through their incident and then their own user_id, with the oldest organization as the fallback for an incident whose users are gone. No column DEFAULT.",
			UpSQL:       breachResponseTenantScopeUp,
			DownSQL:     breachResponseTenantScopeDown,
		},
		{
			Version:     148,
			Name:        "temp_access_tenant_scope",
			Description: "Put temp_access_links under the FORCE RLS belt and give temp_access_usage an org_id — the PAM temporary vendor access surface, where a link grants an outside party SSH/RDP/VNC into an internal host and a usage row records who redeemed it, from which IP, with what user agent. v71 already closed the cross-tenant IDOR on the links table by adding org_id and org-filtering the handlers, and it wrote down why it stopped short of the belt: the public token-redemption path runs with no authenticated org context, so FORCE RLS would fail closed and break redemption for the vendor, and 'the belt would add no protection there' since every management path was org-filtered in code. The first half was true and is now obsolete — a lookup keyed on a globally-unique secret before the tenant is known is a shape this branch has met four times, and v145's magic-link token is the same shape exactly, redeemed under orgctx.WithBypassRLS and pinned by TestPreResolutionLookupsUnderRLS. The second half is the claim the register programme answers: the belt guards the NEXT query, not the audited ones. That query was already present — temp_access_usage, created by v54, has no tenant column at all and handleGetTempAccessUsage reads it by bare link_id, safe only because a separate EXISTS statement runs first, the same implicit-scoping-through-a-joined-set shape v147 found in ai_intelligence.go and v143 in social_providers. Usage rows are attributed through their link, whose org_id has been NOT NULL since v71. The expiry sweep in internal/governance/jit_expiry.go is a third pattern and stays install-wide on purpose — a link past its expiry is expired in every tenant — so it runs explicitly bypassed rather than by accident. Down removes both belts and the usage column but keeps temp_access_links.org_id, which is v71's and not this migration's to drop. No column DEFAULT.",
			UpSQL:       tempAccessTenantScopeUp,
			DownSQL:     tempAccessTenantScopeDown,
		},
		{
			Version:     149,
			Name:        "legal_hold_tenant_scope",
			Description: "Add org_id + FORCE RLS to recording_legal_holds (v42, remote support) and guacamole_recording_legal_holds (v68, PAM) — the two tables that mark a session recording as evidence. This batch destroys rather than discloses, which is a first for the programme: sweepExpiredRecordings selects purge candidates with NOT EXISTS over the hold table, so releasing a hold is what lets the next sweep delete the recording. HandleReleaseLegalHold took a BARE session id, so an administrator of one tenant could release another tenant's litigation hold by naming their session id, and the recording it protected was gone at the next sweep — irreversible, and invisible to the tenant that placed the hold beyond a released_by user id that is not in their organization. Place and list were equally bare, exposing the hold reasons, which are free text describing an investigation. The Guacamole twin is the reason the gap is legible: its handlers do the identical job and every one gates on guacSessionVisible first — two implementations of one control side by side, one guarded and one not, the shape v146 found in mfa_management.go. That guard was also thinner than it looked: guacSessionVisible carried no tenant term and its comment sourced the scope from 'RLS on guacamole_sessions', which holds on a correctly configured connection and not on one with BYPASSRLS, so the check now carries the organization itself and the belt is the second layer rather than the only one. The retention sweeps stay install-wide and already run under orgctx.WithBypassRLS. Holds are attributed through their session, with the oldest organization as a load-bearing fallback because both session tables allow a NULL org_id on old rows. The active-hold partial unique indexes are deliberately NOT re-scoped: they key on session_id, which already determines the organization, so a per-org key would let one session carry two active holds — the v146 UNIQUE(user_id) judgement reached the same way. No column DEFAULT.",
			UpSQL:       legalHoldTenantScopeUp,
			DownSQL:     legalHoldTenantScopeDown,
		},
		{
			Version:     150,
			Name:        "remote_support_session_scope",
			Description: "Backfill, pin NOT NULL and belt remote_support_sessions.org_id — an administrator watching or driving an end user's screen, with the consent state and the recording location on the row. The table carried org_id since v92 and never got the belt; it held the largest count on the needsBelt register, fourteen unscoped queries, and the worst was HandleListSessions: `SELECT ... FROM remote_support_sessions s ORDER BY s.started_at DESC LIMIT 200` with no WHERE clause at all, putting every tenant's remote support history — whose screen was taken over, by which administrator, when, and whether a recording exists — on any tenant's console with no belt behind the omission. THE NULLABLE COLUMN IS THE HAZARD: v92 added org_id without NOT NULL and HandleStartSession wrote whatever getOrgID returned, so belting it would HIDE those rows rather than scope them — the administrator who started a NULL-org session could not list, end or revoke it while the session kept running from the broker's memory, which is the warning the register already carries against edr_device_mappings. The backfill and the NOT NULL therefore precede the policy, and the handler now refuses a session with no organization instead of writing NULL. Queries split three ways as in v148 and v149: admin paths take an explicit org predicate; device paths (consent grant/deny, the agent's own active-session lookup, markActive, touchSession, endSession) authenticate as a device with no tenant on the request and run bypassed on the session or agent key, like v145's magic-link redemption — belting the agent's poll without the bypass would leave a device that can never be helped; and expireOrphanSessions stays install-wide, because a stalled session the sweep cannot see never ages out. Sessions are attributed through the administrator who started them, with the oldest organization as the fallback. Down keeps org_id, which is v92's column, and only lifts the NOT NULL. No column DEFAULT.",
			UpSQL:       remoteSupportSessionScopeUp,
			DownSQL:     remoteSupportSessionScopeDown,
		},
		{
			Version:     151,
			Name:        "guacamole_connection_scope",
			Description: "Give guacamole_connections org_id and the FORCE RLS belt, and drop the dead guacamole_connection_pool table. A guacamole_connections row is the definition of a privileged session target — host, port, protocol, the vault secret injected into the session, and whether an approval, a moderator or a recording is required — and it has never carried a tenant. v59 added the vault_secret_id column to it, belted the two tables it created alongside, and wrote 'guacamole_connections is NOT belted (only ALTERed)' with no reason. What that left reachable is an escalation rather than a disclosure: POST /guacamole/connections/:routeId/connect carries no admin gate, resolves the caller's organization, refuses when there is none, and then loads the target row `WHERE route_id=$1` without using it — after which the handler reads that row's vault secret under orgctx.WithBypassRLS (deliberately, because the server must be able to inject it), pushes the credential into the broker and returns a connect URL. A user of one tenant holding only another tenant's route id gets a live RDP, SSH or VNC session onto that tenant's host with that tenant's credential typed in. The vault belt is intact and irrelevant: it was bypassed on purpose and the connection row decided which secret to bypass it for. THE PRE-SESSION GATES DID NOT GATE: checkAndConsumeApproval and checkModerationActive both key on (connection_id, requester_id) over belted tables, so each sees only the caller's own organization's rows — which is exactly why they cannot help. The request is opened against the other tenant's connection id and approved by an administrator of the caller's own tenant, so a four-eyes control is satisfied without leaving home; scoping the connection is what gives those two queries something to mean. The list endpoint is the plain read of the same defect, `SELECT ... FROM guacamole_connections ORDER BY created_at DESC` with no WHERE clause and no admin gate, exposing every tenant's hostnames, ports and connection parameters — while the app-publishing routes registered beside it carry the comment explaining why they are admin-gated. handleListMyGuacConnections also claimed 'RLS scopes guacamole_connections via the request context; the explicit pr.org_id predicate is defence in depth', which was inverted: there was no policy, so the predicate it called defence in depth was the whole defence. Connections are attributed through their route, whose org_id is authoritative, with the oldest organization as the fallback for a row written with no route. UNIQUE(route_id) is deliberately left alone: the route already determines the organization, so the key cannot span tenants — the v144/v149 judgement, not v143's. guacamole_connection_pool leaves the register by leaving the schema: nothing calls GetPooledConnection or CleanupExpiredConnections, nothing SELECTs the table, and its only INSERT ends in ON CONFLICT (connection_id) against a column that has a plain index and no unique constraint, so every write since v54 has failed at plan time with 42P10 into a logger.Warn — v67 widened its token column to hold encrypted tokens it has never held. Down restores the pool table in v67's shape. No column DEFAULT.",
			UpSQL:       guacConnectionScopeUp,
			DownSQL:     guacConnectionScopeDown,
		},
		{
			Version:     152,
			Name:        "admin_delegation_tenant_scope",
			Description: "Give admin_delegations org_id and the FORCE RLS belt, and put a tenant term on every query that reads or writes it. An admin_delegations row hands one person a named set of administrative permissions until a date, and it is read by the policy enforcement point itself: PermissionResolver merges it into the permission set RequirePermission then decides on. The comment on that read was copied from the query eight lines above it and lost the thing the comment was about. The role_permissions read says 'already scoped to the caller's org by the explicit r.org_id = $2 predicate, so WithBypassRLS is the correct, leak-free fix' and it is; the delegation read says 'Same RLS reasoning as the role_permissions read above: scoped by the explicit delegate_id = $1 predicate' and it is not — delegate_id is a user id, not a tenant term, over a table that had no tenant column to name. A delegation granted in one organization therefore applied to that user in every organization they could authenticate into. The admin API could write across the same boundary: UpdateDelegation builds UPDATE ... WHERE id = $N with no tenant term and permissions is one of the fields it sets, DeleteDelegation is DELETE ... WHERE id = $1, and CreateDelegation inserted whatever delegate_id the caller supplied — so one tenant's administrator could rewrite the permission list on another tenant's delegation, or mint one naming another tenant's user, and the unscoped read in the enforcement point would honour it. The create path now verifies that the delegate, the grantor and the scope all belong to the caller's organization. Rows are attributed to the delegate, since the permissions are theirs to use, falling back to the grantor and then to the oldest organization. THIS MIGRATION DELIBERATELY DOES NOT TOUCH scope_type/scope_id: PermissionResolver resolves and caches them and RequirePermission compares resource and action only, so a delegation scoped to one group grants its permissions everywhere that permission is checked — a real finding of the same class, left open on purpose because every way of closing it changes what an existing shipped delegation grants, which is a product decision rather than a scoping one. Down drops the column with the belt: a rolled-back binary reads this table with no tenant term at all, so a column it ignores would be worse than none. No column DEFAULT.",
			UpSQL:       adminDelegationScopeUp,
			DownSQL:     adminDelegationScopeDown,
		},
		{
			Version:     153,
			Name:        "risk_policy_tenant_scope",
			Description: "Give risk_policies org_id and the FORCE RLS belt, and put a tenant term on the login path's own read. A risk_policies row is a rule the login path consults -- when this condition holds, require MFA, require step-up, deny, or allow these factors -- and v39 created it with no tenant column. GetEnabledRiskPolicies reads `SELECT ... FROM risk_policies WHERE enabled = true ORDER BY priority ASC` with no predicate, and AssessLoginRisk applies every returned row that matches, so every organization's enabled policies were evaluated against every organization's logins. The direction is toward weaker, which is what makes it sharp: applyRiskPolicyActions REPLACES the allowed-factor list rather than merging it, so at high risk -- where the assessment restricts the second factor to WebAuthn and push, the phishing-resistant ones -- a policy belonging to another organization with mfa_methods ['any'] overwrites that list with one including SMS and email. The matching condition need not be clever: risk_score_min 0 evaluates assessment.Score >= 0, true on every login ever assessed. One row in one tenant, and every tenant's step-up admits an SMS code; deny true on the same condition refuses every login everywhere. A control one organization can weaken for all of them is not a control, and it is v151's cross-tenant require_approval arriving on the authentication path. The comment on the create request's TenantID field claimed 'the policy is org-scoped by the request context' when there was no column and no predicate, and CreateRiskPolicy assigned the caller's tenant_id to the RESPONSE STRUCT and never to the row -- so an API caller who supplied one was handed it back as though it had been recorded. This is the third comment in three batches asserting a tenant scoping that does not exist, after v151's credited-but-absent RLS belt and v152's predicate justification copied without the predicate. List, get, update, delete and toggle all addressed policies by bare id, so any administrator could author, re-point, disable or delete any other tenant's login rules. BACKFILL NEEDS AN OPERATOR: nothing on this row names a tenant -- no user, no route, no parent of any kind -- so every existing policy goes to the oldest organization. A policy that has been applying to every tenant will now apply to one, which is the correct direction because no tenant was ever entitled to have another's rule applied to its logins, but a multi-organization install should re-create the policies it meant to have. Down keeps v39's (enabled, priority) index, which is the index a rolled-back binary wants. No column DEFAULT.",
			UpSQL:       riskPolicyScopeUp,
			DownSQL:     riskPolicyScopeDown,
		},
		{
			Version:     154,
			Name:        "lifecycle_tenant_scope",
			Description: "Give the four joiner/mover/leaver tables org_id and the FORCE RLS belt, and put a tenant term on every query that reads or writes them. lifecycle_workflows and lifecycle_policies are the RULES that decide which accounts get roles added, groups removed, sessions revoked, passwords forced, accounts disabled or accounts DELETED; lifecycle_executions and lifecycle_policy_executions are the logs of what those rules did. v54 and v55 created all four with no tenant column. THE ACTIONS WERE SCOPED AND THE RULE THAT AIMS THEM WAS NOT: every mutation in the subsystem carries a tenant term (UPDATE users SET enabled = false WHERE id = $1 AND org_id = $2, DELETE FROM users WHERE id = $1 AND org_id = $2, the user_roles, group_memberships and sessions writes likewise) and findAffectedUsers selects candidates with AND org_id = $N in all four policy types -- deprovisioning.go even carries a comment recording an earlier fix in this programme, that the org has to travel on the context because users is behind the RLS belt or the leaver disable matches its predicate and affects nobody. Nobody scoped the rule. Every handler addressed these tables by bare id: SELECT ... FROM lifecycle_policies ORDER BY name, UPDATE lifecycle_policies SET ... WHERE id = $N, DELETE FROM lifecycle_workflows WHERE id = $1, SELECT ... FROM lifecycle_policy_executions WHERE policy_id = $1. So a second organization's administrator could open the first's leaver policy, change conditions to inactive_days 0 and actions to delete, and hand it back; the owning administrator then runs what their console still labels 'Stale account disable -- 90 days' and it deletes their directory. The org predicate on the DELETE does not help, because the rows destroyed are the owner's own: a control another tenant can re-aim is armed by its owner and pointed by someone else. The other directions are the same defect from different sides -- DELETE removes another tenant's offboarding rule outright and nothing on the owner's console says the leaver control has stopped existing; execute loads any tenant's policy by bare id and runs it against the caller's users; and the execution logs are a plain cross-tenant read of personal data, because actions_taken records username, action and reason per affected account, so listing another tenant's runs returns their directory with a justification column. BACKFILL narrows from the most specific attribution: rules go to their author's organization via created_by, a workflow execution goes to the organization of the USER IT ACTED ON because the row names that person, falling back to the workflow's organization only when that user is gone, a policy execution follows its policy, and anything still unattributed goes to the oldest organization. One consequence for a multi-organization install: a rule that has been visible to every administrator becomes visible to one, and an organization relying on a rule another authored -- which it was never entitled to -- must author its own. Down keeps v54's and v55's own indexes, which are what a rolled-back binary reads. No column DEFAULT.",
			UpSQL:       lifecycleScopeUp,
			DownSQL:     lifecycleScopeDown,
		},
		{
			Version:     155,
			Name:        "federation_tenant_scope",
			Description: "Give federation_rules and custom_claims_mappings org_id and the FORCE RLS belt, re-scope the install-wide email_domain key, and put a real tenant term on every query. A federation_rules row maps an email domain to the identity provider that authenticates it -- the row deciding where a user typing their address is sent to sign in -- and custom_claims_mappings decides what an application is told about whoever signed in. A LEFT JOIN IS NOT A TENANT PREDICATE: the two reads of federation_rules are the same query one word apart. The login path has `FROM federation_rules fr JOIN identity_providers ip ON fr.provider_id = ip.id AND ip.org_id = $2`, which drops rows failing the condition; the admin list has the identical clause written `LEFT JOIN`, which keeps every row of the left table and merely nulls the right side -- so ip.org_id = $1 stopped being a filter and became a decoration, the list returned EVERY organization's federation rules, and COALESCE(ip.name,'') rendered the foreign ones' provider as an empty string. The code reads as though it were scoped, which is why it survived. AND email_domain WAS GLOBALLY UNIQUE (v54 declared it inline): one organization per domain for the whole installation, so whoever registers example.com first owns it and the next organization to try gets a unique violation reported as a bare 500 with no hint that another tenant holds the name -- the same shape as v138's ispm_rules.check_type and ai_agents.name, re-scoped the same way to (org_id, email_domain). Update and delete addressed rules by bare id, so one administrator could disable or delete another organization's SSO routing and its users would quietly fall back to password; create inserted whatever provider_id the caller supplied with no check that the provider was theirs. custom_claims_mappings is the same defect with nothing behind it: its list, update and delete take a bare application_id or a bare id, so one tenant could add, retarget or remove the claim mappings on another tenant's application -- an identity-forgery primitive, except that a search of the whole tree finds NO READER. The table is written by its own admin CRUD and the console page driving it and by nothing else: no token mint, no /userinfo. include_in_id_token, include_in_access_token and include_in_userinfo name three destinations with no consumer, so an administrator can map department into the ID token as dept, save it, see it listed back, and no token ever carries it. It is scoped rather than dropped -- v151 dropped guacamole_connection_pool because it was never read AND its only write had always failed, whereas this holds configuration an operator really entered -- and the gap is named in the guide and CHANGELOG to be fixed as a feature rather than smuggled into a scoping batch. BACKFILL IS EXACT, NOT INFERRED: both tables have an enforced NOT NULL foreign key to an already-scoped parent, so a rule goes to the organization of the identity provider it routes to and a mapping to the organization of the application it decorates. Down restores v54's install-wide UNIQUE, which is the one statement that can fail: if two organizations have each registered the same domain while v155 was applied -- the whole point of the migration -- the global constraint cannot be recreated and the rollback stops, the same trade v138 made, and the honest one. No column DEFAULT.",
			UpSQL:       federationScopeUp,
			DownSQL:     federationScopeDown,
		},
		{
			Version:     156,
			Name:        "developer_portal_tenant_scope",
			Description: "Give developer_settings and oauth_playground_sessions org_id and the FORCE RLS belt, re-scope the install-wide settings key, and put owner and tenant terms on the playground. ONE ROW FOR THE WHOLE INSTALLATION, KEYED ON A STRING LITERAL: developer_settings declares `setting_key VARCHAR(100) UNIQUE NOT NULL` and both handlers use the constant 'global' -- SELECT setting_value FROM developer_settings WHERE setting_key = 'global', and an upsert ON CONFLICT (setting_key) -- so there is exactly one settings row and every organization's administrators share it. The last one to press Save decides for everybody the maximum API keys per user, which scopes an API key may carry (the defaults include write:users, write:applications and write:provisioning), the webhook IP allowlist, the CORS allowed origins, the default rate limit, and whether sandbox mode is on. Third install-wide unique key this programme has found in two batches, after v155's federation_rules.email_domain and identity_providers.issuer_url, and the second time the key itself rather than a missing predicate was the whole defect; re-scoped to (org_id, setting_key). AND NOTHING READS IT: a search of the tree finds developer_settings in its own two handlers, in the migrations, and nowhere else -- no API-key mint checks APIKeyMaxPerUser or APIKeyAllowedScopes, no CORS middleware consults CORSAllowedOrigins, no limiter reads RateLimitDefault -- so the page presents six security limits, saves them, reads them back and constrains nothing, and the cross-tenant write is only as harmful as the settings are effective, which is not at all. Same shape as v155's custom_claims_mappings and recorded rather than fixed here for the same reason: giving these values a consumer is a feature. THE PLAYGROUND SESSION HOLDS THE SECRETS OF AN OAUTH FLOW -- code_verifier, authorization_code, access_token, id_token -- and the execute handler loaded one by bare id with no tenant term, no owner term and no requireAdmin, though every other handler in that file has one; so the PKCE verifier that lets an authorization code be redeemed was retrievable by id alone. The schema shows what was meant: v54 gave the table user_id UUID REFERENCES users(id) ON DELETE CASCADE and an index on it, idx_playground_user, and the insert has never set that column and no read has ever used it -- an index on a column that is always NULL, which is what an intention looks like after the code that would have honoured it was not written. Both handlers now set and check owner as well as tenant. Backfill: settings go to the organization of whoever last updated them via updated_by, playground sessions to their user_id's organization, which in practice is nobody's, so those fall to the oldest organization and expire in fifteen minutes regardless. Down restores v54's install-wide UNIQUE, which fails once more than one organization has saved settings -- refusing beats deleting somebody's settings to make a rollback succeed. No column DEFAULT.",
			UpSQL:       developerPortalScopeUp,
			DownSQL:     developerPortalScopeDown,
		},
	}
}
