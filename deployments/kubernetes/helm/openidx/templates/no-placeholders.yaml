{{- /*
Interlock only: renders nothing. It walks .Values and fails the render while
any string still carries a REPLACE-WITH marker (see openidx.rejectPlaceholders
in _helpers.tpl for the measurement that motivated it). It lives in its own
file so the refusal does not depend on which resource happens to be enabled.
*/ -}}
{{- include "openidx.rejectPlaceholders" (dict "v" .Values "path" "") -}}
