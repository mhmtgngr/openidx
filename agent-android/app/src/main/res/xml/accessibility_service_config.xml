<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility Service config. We do NOT consume any accessibility events
    when no remote-support session is active — eventTypes is the minimal
    set needed to keep the service registered. The capability that actually
    matters here is canPerformGestures, which is what dispatchGesture()
    requires.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="0"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:summary="@string/accessibility_service_summary"
    tools:ignore="UnusedAttribute" />
