```json
{
  "phases": [
    {
      "order": 1,
      "name": "Audit Stream Origin Validation",
      "priority": "critical",
      "category": "security",
      "description": "Fix insecure CORS in audit SSE endpoint at internal/audit/stream.go:80. The `checkOrigin` function currently returns `true` for all origins, allowing any website to connect to the audit event stream. Implement proper origin validation: parse the Origin header, check against a whitelist of allowed frontends from environment variable `AUDIT_STREAM_ALLOWED_ORIGINS` (comma-separated list), return false for untrusted origins. Add tests for valid/invalid origins.",
      "success_criteria": [
        "Unit tests pass with origin validation",
        "CORS rejection verified for unknown origins"
      ],
      "estimated_minutes": 45
    },
    {
      "order": 2,
      "name": "SMS Rate Limiting",
      "priority": "high",
      "category": "security",
      "description": "Add rate limiting to SMS sending to prevent abuse. In internal/sms/service.go, implement a Redis-backed rate limiter using the existing Redis client. Add `SendSMS` method to check `phone_number` key in Redis with prefix `sms:rate:`. Limit: max 5 SMS per phone per hour, max 10 per day. Return error 429 when limit exceeded. Add environment variables `SMS_RATE_LIMIT_HOUR=5` and `SMS_RATE_LIMIT_DAY=10` for configurability. Add unit tests with mock Redis.",
      "success_criteria": [
        "Rate limiter rejects excess SMS",
        "Tests cover hourly/daily limits",
        "Redis INCR/EXPIRE commands used correctly"
      ],
      "estimated_minutes": 60
    },
    {
      "order": 3,
      "name": "Ed25519 Key Support",
      "priority": "medium",
      "category": "feature",
      "description": "Implement Ed25519 signing algorithm at internal/oauth/keys.go:205. Currently only RSA and ECDSA are supported in `GenerateKeyPair` and `Sign` methods. Add `Ed25519` case to the `Algorithm` enum switch statements. Use `crypto/ed25519` package: `ed25519.GenerateKey` for key generation, `ed25519.Sign` for signing. Update JWKS JSON marshaling to include `crv: \"Ed25519"` and `x`/`y` base64url-encoded fields. Add tests verifying Ed25519 JWT generation and validation.",
      "success_criteria": [
        "Ed25519 keys generate and sign correctly",
        "JWKS endpoint returns Ed25519 keys",
        "JWTs with Ed25519 signatures validate"
      ],
      "estimated_minutes": 50
    }
  ],
  "project_health": {
    "score": 82,
    "critical_issues": [
      "Audit SSE endpoint allows any origin (CORS bypass)",
      "SMS sending lacks rate limiting (abuse vulnerability)"
    ],
    "strengths": [
      "All 31 tests passing",
      "Build compiles successfully",
      "Good test coverage (100 test files)",
      "WebAuthN middleware now has proper auth"
    ]
  },
  "rationale": "Ordered by security risk exposure. Audit stream origin bypass is a critical XSS/data leak vulnerability allowing malicious sites to stream admin events. SMS rate limiting prevents expensive abuse/spam. Ed25519 is a performance/crypto upgrade but not blocking security. Build and tests are healthy, so focus shifts to hardening."
}
```
